node_modules/
.env
.env.local
dist/
build/
coverage/
*.log
.DS_Store

# ローカル秘密控え（🟡 識別子のみ・🔴 token/key は置かない）— commit 禁止
SECRETS.local.md

# ベースルール完全版（内部事例を含むため公開しない）— commit 禁止
.claude/rules/product-baseline.full.md

# 実行の証跡。動画は既定で Git に入れない（C29 / Issue 007）。
# 実測で 20 ケース × 20 営業日 = 月 2.12 GB。Git の履歴に入る以上ずっと累積するので、
# Free / Pro の LFS 無料枠 10 GiB は 5 か月で尽きる。帯域はもっと早く、
# 溜まった履歴を 1 回 clone しただけで月間 10 GiB を超える。
runs/
