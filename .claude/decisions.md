# git-qa — 決定事項（叩き）

- 作成: 2026-08-14
- 出典: 原案 2 本 + 2026-08-14 のプロダクトオーナーとのやり取り

---

## 確定済み

### C1: 結果の語彙は `VERIFIED` / `AUTO_PASS` を分ける

`VERIFIED` = 人が名前を置いた。`AUTO_PASS` = AI が通しただけで誰も見ていない。

**Why**: 原案 `mobile-qa-runner` の素の `PASS` だと 2 つが同じ値に潰れる。この製品の存在理由は「人が保証したこと」を残すことなので、潰した時点で意味がなくなる。`git-qa-expanded` 側の語彙が正しい。

---

### C2: 不正防止機構を作らない（非目標に明記する）

中身を見ずに `VERIFIED` を置くことはできてよい。

**Why**: 証跡は偽装を防ぐためのものではない。**置いた瞬間にその人の名前が残る＝ロックではなく署名。**丸投げして事故が起きたときに困るのは置いた本人なので、道具側で防ぐ必要がない。明記しないと実装者が善意で承認フローを作り始め、**楽をするための道具が重くなる。**

---

### C3: TSV へ書き戻さない

git-qa は TSV を読むだけ。結果は `run.json` に出す。TSV への反映は md-business が行う。

**Why**: (1) TSV の書式規約（行 ID・`computed`・`hidden`）を git-qa が抱えずに済み、md-business の仕様変更に追随不要になる。(2) md-business を持たない利用者が自前で書くと、集計列を実数で潰す・控えの行を復活させるといった壊し方をする。**読める形のまま壊れるので気づけない。**

同一のエージェントが git-qa と md-business の両方を操作するので、橋渡しはエージェント側で成立する（2026-08-14 決定）。

---

### C4: 実行中の主媒体はライブビュー。動画は保管物

UI の中心は録画プレイヤーではなくミラーリング。Android は scrcpy を前提とする。Appium のスクリーンショット連写は使わない。

**Why**: 人は AI が操作しているのをその場で見ている。動画は「あとでどうだったっけ」と第三者提出のためのもの。**遅延が 1〜2 秒あると「固まったのか遅いだけか」が分からず、人が手を出す。**その時点で道具の目的が達成されない。

---

### C5: 人が見るのは「AI 判断保留」「差分」「失敗」の 3 つだけ

全ケースの人間確認を強制しない。

**Why**: ノルマ化すると使われなくなる。ASSIST の本体は「AI が全部やる」ではなく「AI が見る場所を絞る」。

---

### C6: `VERIFIED` は 1 打鍵で置け、置いたら次ケースへ自動で進む

**Why**: 判断が正しくても、そこに至る手数が多ければ楽になっていない。動画をシークしてボタンを探す導線を作った時点で手作業より遅くなる。

---

### C7: 主指標は「同じケース群の所要時間（手作業比）」

同じ 20 ケースを手で通した時間と、この道具で通した時間を測る。後者が明確に短いこと。

**Why**: 原案 `git-qa-expanded` §22 の「従来の手動チェックより明確に楽」は主観のままで判定できない。**この数字が出ないなら、他が全部できていても MVP ではない。**

---

### C8: Adapter に入るかは「ライブ映像を出せるか」で決まる

Web / Android / デスクトップアプリは入る。API / CLI / バッチは入らない（別の見せ方が要る）。iOS は D1 の結果次第。

**Why**: ライブビューが芯（C4）である以上、映像が出せない対象では ASSIST が成立しない。原案 `git-qa-expanded` §21 は Phase 8 で API / CLI を同じ枠に足そうとしているが、**同じ Adapter には入らない。**

---

### C9: 測るのは iOS が先。作るのは Android が先

Phase 0 で Windows→iOS のライブビュー可否だけを測る。実装の MVP は Windows + Android から。

**Why**: 未知は Windows→iOS だけで、そこだけが製品の形を変える。Android は既にできると分かっており、macOS なら Android も iOS もできる算段がある＝**最悪ケースでも製品は死なない。**原案 `mobile-qa-runner` §4 は同じ事実を「だから全体の成立条件にしない」＝後回しの根拠に使っているが、**死なないからこそ先に試せる。**分岐が早く判れば設計を戻さずに済む。

---

### C10: OSS 公開物に実証跡を混ぜない

サンプル・ドキュメントはダミー案件で作る。OSS にする場合は commit history に個人名・個人メールも残さない。

**Why**: 証跡には実画面が写る。顧客へ返すもの自体は問題ないが、公開リポに混ざると回収できない。

---

### C11: 録画の on/off は実行開始時の設定。モードに紐づけない

**Why**: どのモードで録画が要るかは、その時の人が決めること（2026-08-14 決定）。道具側で「AUTO なら録画」と決め打つのは越権。

付随して必須になるもの: **`run.json` に「録画しなかった」を残す。**動画が無いときに「オフで走らせた」のか「録画に失敗した」のかを区別できないと、後から証跡として読めない。

---

### C12: Web はデスクトップアプリと同じく「ついで」で足す

Adapter が効いていれば追加コストが小さい。ただし**最初の正式対象にはしない。**

**Why**: 原案 `git-qa-expanded` §5 は「WebはWindows版MVPの最初の正式対象とする」と書いているが、これは**作りやすい順**であって痛い順ではない。順序は C9 に従う。

---

### C13: 挑戦して駄目だった場合、Windows 版は iOS なしで出す。macOS を MVP の前提にしない

**まず挑戦する（Issue 001）。**そのうえで Windows→iOS が不成立だった場合に、**Windows 版は iOS を含まない形でそのまま出す。**macOS 専用にはしない（2026-08-14 決定）。

**これは「iOS を諦める」決定ではない。**（2026-08-14 確認）

| 誤読 | 正しい読み |
|---|---|
| iOS はスコープ外なので Issue 001 は省略してよい | **Issue 001 は必ず実施する。**省略は No-Go ではない |
| 難しそうなので早めに切り上げてよい | **駄目だと分かるまで試す。**判定を出すことが完了条件 |
| iOS が載らない前提で設計してよい | 載る前提で Adapter 境界を引く（Issue 002）。載らないと分かったときに外す |

**「載せない」は判定の結果であって、着手前の前提ではない。**Conditional（条件付きで見られる）が出た場合は、**条件を潰す方向で検討する。**そこで即座に落とさない。

**Why**: mac を前提にすると、**Windows で動く道具という前提そのものが崩れる。**初期の企画草案 §4 が「macOS Adapter があるから全体の成立条件にしない」と書いたのは正しいが、その先を「不成立なら macOS へ乗り換える」と読むと本末転倒になる。**乗り換えるのではなく、載せないだけ。**

帰結:

- **Issue 001 の優先度は下がらない。**最初に着手する（Phase 0）
- **結果でロードマップも設計も揺れない。**決まるのは「Windows 版に iOS が載るかどうか」だけ
- 不成立でも Web / Android / デスクトップアプリで製品としては成立する（C8）
- macOS 対応は将来の別枠。**MVP には入れない**
- **機材の判断には繋がらない**（No-Go が出ても mac を買う話にはならない）

---

### C14: 製品名・リポ名は `git-qa`

初期の企画草案 §27 の第一候補を採る（2026-08-14 決定）。`qa-runner` / `qa-evidence` は不採用。

**Why**: 対象が Web / デスクトップまで広がるので、mobile を含む名前は使えない。`git-qa` は「証跡が Git に残る」という一番外しにくい特徴を名前にしている。

---

### C15: OSS として公開する（2026-08-15 確定）

`meta-taro` 配下の OSS 系列（`dbboard` / `md-business`）に並べる。**D2 は解決。**

**Why**: 公開して差し支えのある要素が無く、掃除のコストも公開直前の squash 1 回で足りる。決めた以上、**最初から公開前提の作法で作れる**（後から倒すより安い）。

**この決定で必須になるもの**:

- **個人名・個人メールの検出 CI を最初から入れる**（Issue 003�）
- **サンプル・テストデータはダミー案件で作る**（C10）
- **証跡リポを本体と分ける**（C16）
- README・ドキュメントに**顧客名・対象サイト名・案件名を書かない**

---

### C16: 本体（公開）と証跡（非公開）を必ず別リポにする

C15 の帰結。**同じリポに入れてはいけない。**

| | history | 公開 |
|---|---|---|
| **本体** `meta-taro/git-qa` | 要らない。公開直前に squash してよい | **する** |
| **証跡**（実運用） | **history が中身。**消せない | **しない** |

**Why**: 「証跡が Git に残り、第三者が後から遡れる」がこの製品の機能そのもの（名前の由来・C14）。証跡リポの history を消すと売りが消える。**一方で本体は history を消せる。消せる側と消せない側を混ぜると、消せない側に消したいものが入る。**

**未定**: 証跡リポの名前・置き場所（顧客ごとに作るか 1 本か）。**本体の作業は止まらない。**

---

### C17: `VERIFIED` を AI の型に入れない（2026-08-17）

C1 の実装。結果の語彙を 1 つの型で持たず、**誰が置いたかで型ごと分ける。**

| 定数 | 中身 | 置く人 |
|---|---|---|
| `AI_RESULTS` | `PASS` / `FAIL` / `BLOCKED` / `SKIP` | AI |
| `HUMAN_RESULTS` | `VERIFIED` / `FAIL` / `BLOCKED` / `SKIP` | 人 |
| `CASE_RESULTS` | 上記の合併（`AUTO_PASS` を含む 5 値） | 出力される値 |

`AUTO_PASS` はどちらの入力にも無い。**`resolveCaseResult` が「AI が `PASS` と言い、人が何も置いていない」場合にだけ作る値**であって、誰かが直接置ける値ではない。

**Why**: C1 を enum 1 本で表すと、AI 側の実装が `VERIFIED` を代入できてしまう。禁止をレビューで守らせる形にすると、いずれ通る。**型として持たせなければ、書こうとした時点でコンパイルが落ちる。**

---

### C18: `run.json` の正本は JSON Schema ファイル。TypeScript の型は写し（2026-08-17）

`packages/core/schema/run.schema.json`（draft 2020-12・`$id: urn:git-qa:schema:run:v1`）が正本。検証は ajv で行う。TS の型は編集の助けであって、検証はしない。

**両者がずれていないことをテストで見る**（TS の定数配列と Schema の `enum` を突き合わせる）。

**Why**: `run.json` は git-qa の**外**へ出る（証跡リポ・md-business・第三者のレビュー）。読む側は TypeScript を持っているとは限らない。**外に出る形の正本を、こちらの言語の型にしない。**`$id` を GitHub URL にしないのは、公開先のオーナー名を実装が先回りして決めないため（C15 の置き場は決まっているが、URL を焼くと移せなくなる）。

---

### C19: 「どの機体・どのビルドで走らせたか」は TSV でなく `run.json` に持つ（2026-08-17）

検証シートに `対象機種` のような列を足さない。`run.json` の `target`（`kind` / `device` / `build`）に持つ。

**Why**: 同じシートを Pixel と iPhone と別ビルドで**何度も**走らせる。シートに書くと 1 枚が 1 機体に縛られ、機体の数だけシートが増える。**シートは「何を確かめるか」、run は「いつ・どれで確かめたか」。**C3（TSV へ書き戻さない）とも整合する — 列を足せば、いずれ埋めたくなる。

---

### C20: 録画は 4 状態で持つ。「録画していない」と「録画に失敗した」を同じ値にしない（2026-08-17）

`recording.state` は `recorded` / `not_requested` / `failed` / `unsupported`。`failed` と `unsupported` は `reason` が必須、`not_requested` には `reason` を書けない。

**Why**: C11 で録画は実行開始時の設定になった。**動画が無いときに「オフだった」のか「録れなかった」のかが分からないと、証跡として提出したときに説明できない。**「ファイルが無い」で表すと必ず潰れるので、状態を明示的に持つ。

---

### C21: 検証シート TSV は CRLF のまま保つ（2026-08-17）

`.gitattributes` で `*.tsv -text`。git の改行正規化から外す。

**Why**: md-business の `test-spec-tsv/v1` は CRLF が仕様のうち。正規化されると「パーサは通るが元ファイルと 1 バイト違う」状態になり、C3（読むだけで書き換えない）を確かめるハッシュ比較が意味を失う。

---

### C22: 自動整形はコードだけにかける。Markdown と TSV にはかけない（2026-08-17）

`.prettierignore` に `*.md` と `*.tsv`。

**Why**: `.claude/rules/product-baseline.md` と `.claude/templates/` は上流から配布される。整形をかけると、次の配布のたびに中身と無関係な差分が出て、本当の変更が読めなくなる。TSV は列区切りが仕様なので、整形すれば単に壊れる。

---

### C23: install script は `esbuild` だけ通す（2026-08-17）

`pnpm-workspace.yaml` の `allowBuilds` に `esbuild` のみ。他は既定どおり全て止める。

**Why**: vitest → vite が依存し、postinstall がプラットフォーム別バイナリを配置する。**開発時にしか使わず、成果物には入らない。**通す理由をここに書けないものは通さない。

---

### C24: Adapter は「ライブ映像を出せる」ことを型で必須にし、画面の状態は生データのまま渡す（2026-08-17）

`TargetSession.liveView` は省略できない。`Observation.raw` は `unknown` で、コアは解釈しない。詳細と Web を当てた結果は `docs/adr/0001-adapter-boundary.md`。

**Why**: C8 の線引き（ライブ映像を出せるものだけが Adapter に入る）を文書に書くだけでは守られない。任意にすれば、映像を出せない対象は「実装しなければいい」だけで入ってくる。**入れられない形にする**のが唯一効く（C17 と同じやり方）。状態のほうは逆で、DOM とアクセシビリティツリーは同じものの方言ではないので、共通の型へ潰すと潰した時点で情報が落ちる。

---

### C25: テスト用の Fake アダプタを `packages/core` に同梱して輸出する（2026-08-17）

`createFakeAdapter()` と契約テスト `test/adapter/contract.ts` を core 側に置く。

**Why**: 実機・adb が無くても芯（人が見て保証する流れ）を通しで動かせるようにするため。下流のパッケージが対象なしでテストを書くのにも要る。契約テストを「実装ごとに当てる束」として置いておくと、Android / Web を足すときの答え合わせがそのまま走る。

---

### C26: 実行器は「繋ぐ前に落とす・途中では落ちない」（2026-08-17）

`executeRun()` の失敗の置き方を 2 つに分けた。

- **繋ぐ前に落とす**: シートの不備（`No.` の重複 / 非整数 / `項目` が空 / 列の欠落）、`mode` と人へ聞く口の食い違い。**繋いでから落ちると、端末を掴んで何もせず離すことになる**
- **途中では落ちない**: ケースの実行が例外を投げても `FAIL` として残し、後続を続ける。録画の停止が失敗しても合否は残し、`{state:'failed', reason}` に理由を書く

**Why**: 途中で throw すると、そこまで走らせた分の証跡ごと消える。**1 件落ちたことより、走った 20 件が残らないことのほうが痛い。**

**併せて決めたこと**

- 録画は**ケースの操作までで閉じる**。人が考えている時間を動画に入れない（後から見返すとき、無音の停止画が延々と続くだけで読めなくなる）
- `recording.requested` と `target` は**セッションから取る**。呼び出し側に二重に書かせない（食い違ったときにどちらが正か決まらない）
- `run.json` は**スキーマを通らなければ書かない**・**同じ `runId` に 2 度書かない**。壊れた証跡と、黙って消えた証跡を作らない

---

### C27: ライブ映像はストリームを取り込んで自前で描く。骨格は Tauri（2026-08-17）

**他プロセスの映像を遅延なくウィンドウ内に出せるか**が、この製品の骨格に固有の要件（C4）。骨格を選ぶ前に実測した。

- **方式 A（生 H.264 を受け取り、アプリ内で復号して canvas に描く）**を採る。枠の中に自由に置け、ケース一覧・判定欄と重ねられる
- 骨格は **Tauri**。Windows の WebView2 ＝ Chromium で、下の実測がそのまま当てはまる。Rust 側で scrcpy / adb の受け口を持てる（C24 の実装先として自然）

**実測**（Windows / Chromium・31 秒）: 受け取り 1892 / 描画 1891（取りこぼしなし）・60.3 fps・復号の待ち 2.9 ms・最初の絵まで 648 ms。

**実機は使っていない。**scrcpy が吐く形（生 H.264 Annex-B の 1 本のストリーム）を ffmpeg で作って代役にした。**確かめたのは端末との接続ではなく、映像を GUI の中に出せるかだけ。**

詳細・弱いところ・覆すべき条件は `docs/adr/0002-live-view-embedding.md`。**macOS / Linux は未測定で、そこが駄目なら Tauri の判断は覆る。**

---

### C28: デスクトップ骨格の作り（2026-09-01）

C27 で決めた Tauri の中身を、`packages/desktop` として置いた。作りで決めたのは 4 点。

- **フロントのビルドは Vite。**`vite` は既に vitest 経由で依存グラフに入っており（`vite@7.3.6`）、明示 devDependency にしても**新規に増えるパッケージが無い**。`^7.3.6` で留める（既定で解決される 8 系を入れると vite が 2 メジャー同居し、上の根拠が崩れる。vitest 3.2 の peer も `^5 || ^6 || ^7`）
- **DOM のテストは happy-dom。**「空の 3 カラムが出る」を実際の DOM で検査する。純粋な関数だけを検査する形にすると、**画面が出ていないのにテストが通る**状態を作れてしまう
- **3 カラムの構成は TypeScript 側（`src/columns.ts`）に一本化する。**幅は CSS へ `--column-flex` として渡し、CSS に数字を書かない。2 箇所に同じ数字を置くと、片方だけ直された状態に気づけない
- **`tsc` は型検査だけに使う**（`emitDeclarationOnly`）。配るものは Vite が `dist/web` へ出す。同じ JS を 2 系統から吐かせない

**中央（ライブビュー）が左右より広いことを、テストで固定した。**C4 / PRD §2 のとおりここが主媒体で、中央が狭くなる変更は見た目の調整ではなく製品の前提を崩す変更なので、テストで落とす。

**色・タイポグラフィは決めていない。**`DESIGN.md` が全項目空で、見た目は人が決める領域（product-baseline §11）。骨格は独自の色を持たず、枠線は `currentColor` から作り、配色は OS の設定に従う。**アイコンも Tauri の既定のまま。**ここを AI が埋めると、そのまま既成事実になる。

**Cargo の `authors` は置かない**（product-baseline §25）。アプリ識別子は `dev.gitqa.desktop`。既定の `com.tauri.dev` は Tauri 側の予約値で、そのままでは配布物を作れない。

---

### C29: 動画は既定で Git に入れない。録画そのものは既定 on（2026-09-02）

Issue 007 の試算から。**Git LFS の無料枠は、この製品の証跡量に対して小さすぎる。**

**2026-09-02 に Android エミュレータで実測し、数字を差し替えた。**
最初は scrcpy の既定ビットレート 8 Mbps（上限）で置いて「24 GB / 月・約 9 営業日で無料枠が尽きる」と
書いたが、**実測は 11 分の 1 だった。**

実測（scrcpy 4.1・Pixel 3a / API 31 エミュレータ・設定画面をスクロール・実時間 60 秒）:
**5.05 MB / ケース**（1.28 Mbps の映像区間 / 0.71 Mbps の実時間平均）。

**scrcpy は画面が変化したときしかフレームを作らない。**8 Mbps は上限であって実際に出る値ではない。
**容量は実時間ではなく画面が変化した量に比例する**ので、人が考えている時間・AI が待っている時間は
ほぼ容量を食わない。

| | 上限で置いた場合 | **実測ベース** |
|---|---|---|
| 20 ケース × 20 営業日 | 24 GB / 月 | **2.12 GB / 月** |
| 無料枠 10 GiB（Free / Pro）まで | 約 9 営業日 | **累積で 5.1 か月** |
| Team / EC 250 GiB まで | 11.2 か月 | **累積で 127 か月** |

**それでも決定は変わらない。**

- **ストレージは Git の履歴に入る以上、青天井に累積する。**月 2.12 GB でも無料枠は 5 か月で尽きる。
  **どんなに小さい月次でも、固定枠はいつか超える**
- **帯域のほうが先に破裂する。**5 か月ぶん（10.7 GB）を溜めたリポを**1 回 clone しただけで
  月間帯域 10 GiB を超える。**開発者が 1 人増えるたびに起きる
- **ただし Team / Enterprise Cloud（250 GiB）なら 10 年以上もつ。**そちらでは LFS は現実的な選択肢で、
  「LFS は無理」ではなく「無料枠では無理」が正しい

**決めたこと**

1. **動画は既定で Git に入れない。**`runs/` を `.gitignore` に入れ、ローカル保管を既定にする
2. **録画そのものの既定は on。**保管が Git の外なら、費用は課金ではなくディスク。**off にする口は必ず出す**（C11）
3. **古い Run を自動で消さない。**消すのは人の判断（消すと、なぜそう判定したかの根拠が消える）
4. **Git LFS はオプトイン。**使う場合の試算と「単価は料金計算機で確認すること」を文書に書く

**Why**: 費用は自社なら案件原価として説明が付くが、**OSS の利用者にはそのまま降りる**（Issue 007）。
既定で課金経路に乗せる設計にしない。**既定を「入れない」にしたので、超過単価が分からなくても
既定の月額は 0 で確定する。**

**費用を理由にライブビューや証跡そのものは削っていない。**削ったのは動画の保管先だけ
（Issue 007 の注意書き）。`run.json` とケースの合否は従来どおり残る。

**この決定の弱いところ**

- **実測はエミュレータであって実機ではない。**GPU は swiftshader、内容は設定画面のスクロールだけ、
  端末は 1 台（API 31）。**写真・動画・アニメーションの多い実アプリはもっと出る。**
  真の値は上限 8 Mbps と実測 0.71 Mbps の間のどこかで、**実機での測り直しが要る**
- **超過単価が一次情報から取れていない。**docs.github.com に記載が無く、料金計算機へ誘導される。
  加えて `github.com/pricing` は docs 側が「廃止した」と書くデータパックを今も表示しており、
  **一次情報どうしが食い違っている**

**これを覆すべき条件**

- 実機の scrcpy で録った実測が、**Free / Pro の無料枠に累積でも収まる**と分かった（エミュレータの実測では 5.1 か月で尽きる）
- 利用者から「証跡が Git に無いと運用にならない」という要求が実際に出た（そのときは LFS ではなく
  別ストレージを含めて置き方を選び直す）

---

### C30: 契約テストと fixture を `@git-qa/core/testing` から輸出する（2026-09-02）

Adapter の契約テスト（`describeAdapterContract`）は `packages/core/test/` の下にあった。
**下流のアダプタは別パッケージになるので、そこからは当てられない**（TypeScript の
project references が、参照先プロジェクトのファイル一覧に無いファイルの import を拒む）。

`src/testing/` へ移し、`package.json` の `exports` に `"./testing"` を足した。

**Why**: Fake アダプタを輸出しているのと同じ理由。「本番の実行では使わないが、下流の
パッケージが対象なしでテストを書くために要る」。**契約テストは当てられて初めて意味がある**
（Issue 002 の「実装ごとにこの束を当てる」）。

**弱いところ**: `src` の下に vitest を import するファイルが入る。本番の実行では
読み込まれないが、**公開している入り口が 1 つ増えた**ことは事実。

---

### C31: Android アダプタは adb / scrcpy の呼び出しを注入できる形にする（2026-09-02）

`packages/adapter-android`。**Adapter は差し替える前提なので別パッケージ**（product-baseline §9）。

- **プロセスを起動する部分を 1 枚のポート（`CommandRunner`）の向こうへ追い出した。**
  本物の起動は `node-runner.ts` だけが知っている
- **純粋な部分を `adb.ts` に集めた**（引数の組み立て・端末一覧の解析・`input text` の escape・
  uiautomator の dump から座標を出す）。**端末が無くても検査できるのはこの層**
- 実機に当てる検査は `GIT_QA_ANDROID_E2E=1` のときだけ走る（§4「繋がらない環境でも走る形にする」）

**Why**: これで**契約テストを端末なしで当てられる**。当てられなければ、契約テストは
「Fake が Fake を満たす」ことしか言っていない。

**併せて決めたこと**

- **要素の指定は完全一致のみ**（`resource-id` → `text` → `content-desc`）。部分一致にすると、
  画面が変わったときに別の要素を掴んで、しかも気づけない
- **空の参照は弾く。**`resource-id=""` のノードは画面上に多数あり、空文字を許すと
  「最初に見つかった何か」を掴む。**テストがこのバグを捕まえた**
- **非 ASCII の `input text` は落とす。**端末の `input text` は IME を経由しないので日本語を
  送れない。黙って化けた文字を打つより、送れないと言うほうがよい
- **ライブビューと録画は別プロセス。**人が窓を閉じても録画は続くべきで、逆も同じ
- **端末が複数見えていて serial の指定が無ければ、選ばずに落とす。**黙って 1 台目を選ぶと、
  証跡に書いた相手と実際に触った相手がずれる

**この決定の弱いところ**

- **当てたのはエミュレータであって実機ではない。**メーカー独自の UI・uiautomator が返さない
  要素・USB の切断といった実機固有の癖はここでは出ない
- **ライブビューは scrcpy の別窓**（`external-window`）。C27 で決めた「アプリの枠の中に描く」
  方式にはまだ繋いでいない。**Issue 003 の画面と繋ぐのはこれから**

---

### C32: 端末の生 H.264 は `adb exec-out screenrecord` から取る（2026-09-02・暫定）

D6 の決着。**scrcpy のサーバへ直接繋ぐ（A）のではなく、Android 標準の `screenrecord` に
生 H.264 を吐かせる（B）。**

**Why**: PRD §2 の「scrcpy 前提」と §11 の非目標「scrcpy の再実装」が正面から当たっていた。
**先に安いほう（B）を実装して測り、足りなければ A へ進む**という順にした。実装は数十行で、
端末へ何も送り込まない。

**実測（Pixel 3a / API 31 のエミュレータ・操作を流しながら）**

| | 値 |
|---|---|
| 受け取り | 1,216,253 バイト / 32 枚 / 4,793 ms |
| 最初の 1 枚まで | **491 ms** |
| 実効ビットレート | 約 2.0 Mbps |

`adb` → 生 Annex-B → `createAnnexBSplitter` → IDR を含む枚、まで通った。

**`--output-format=h264` は `screenrecord --help` に載っていない**（AOSP の隠しオプション）。
実際に叩いて確認している。

**暫定である。A へ戻る条件を残す**（詳細は `docs/adr/0003-live-view-transport.md`）。

- Issue 005 で測った**操作から表示までの遅延**が上限を超えたとき
- `--output-format=h264` が通らない端末が出たとき（隠しオプションなので保証が無い）
- 180 秒ごとの繋ぎ直しが、実運用で目に見える切れ目になったとき

**併せて決めたこと**

- **`LiveViewTransport` に `h264-stream` を足した**（この型は「対象を足すときに増やす」設計）。
  **映像を読む口（`frames()`）は `h264-stream` のときだけ生やす。**別窓の実装に空の口を
  持たせると、**映像が来ないのか、そもそも通らない方式なのかが区別できなくなる**
- **どちらの方式を使うかは呼ぶ側が選ぶ。**既定は従来どおり別窓。遅延を測ってから既定を変える
- **生 H.264 の切り出し（`createAnnexBSplitter`）はコアへ移した。**UI 固有ではなく、
  映像を出す側（アダプタの検査）も描く側（デスクトップ）も同じ切り方を使う

**この決定の弱いところ**

- **測ったのは「繋いでから絵が出るまで」であって、「操作してから画面に出るまで」ではない。**
  人が待つかどうかを決めるのは後者（Issue 005）
- **エミュレータであって実機ではない**
- **180 秒の上限を跨ぐ繋ぎ直しを実装していない**
- **`webcodecs.ts` の codec 文字列が固定**（`avc1.42E01E`）。端末が吐いた SPS は Level 5.0 で
  食い違っている。**本来は SPS から組み立てるべき**

---

### C33: Node 側と画面側は localhost の HTTP で繋ぐ（2026-09-02）

`packages/live-bridge`。アダプタ（Node で動く）が流す生 H.264 を、画面（webview で動く）へ渡す。

**Why HTTP**: スパイクで実測した経路をそのまま使える（ADR 0002）。**Rust を書かずに済み**、
`fetch` のストリームで読めるので受け手側に依存が要らない。

**守りを 2 つ入れた。**ここを流れるのは**検証中の端末の画面**で、顧客名や電話番号が写る（PRD §10）。

1. **127.0.0.1 でだけ待ち受ける。**外から届く口にしない
2. **URL に無作為の 32 文字を入れる。**同じ PC の別プロセスから当てずっぽうで覗けないように。
   **localhost なら安全、ではない**

受け手側も、**localhost 以外・http 以外の URL は受け取らない**（`liveStreamUrlFromLocation`）。

**実測（Pixel 3a / API 31 のエミュレータ）**

| 経路 | 値 |
|---|---|
| `screenrecord` 直結 | 1,158,406 バイト / 32 枚 / 最初の 1 枚まで 370 ms |
| 端末 → アダプタ → 橋 → 受け手 | 777,238 バイト / 20 枚 / 最初の 1 枚まで 641 ms |

**併せて直した 3 つ**

- **`@git-qa/core` の入口が Node 専用のファイル IO を巻き込んでいた。**webview 側から使うと
  バンドルできない（`node:fs` が解決できず**実際にビルドが落ちた**）。
  **ブラウザでも読める入口 `@git-qa/core/live` を分けた**
- **`pnpm verify` に `build` が入っていなかった。**上のビルド失敗を、テストが全部通ったまま
  取り逃がしていた。**`verify` の最後に `build` を足した**（CI もこれを走らせる）
- **実機に当てる検査は 1 台の端末を取り合う。**ファイルを並行させると、片方の操作が
  もう片方の `uiautomator dump` を壊す（**実際に落ちた**）。`GIT_QA_ANDROID_E2E=1` のときだけ
  ファイルの並行を止める

**この決定の弱いところ**

- ~~橋を起こして URL を画面へ渡す「宿主」がまだ無い~~ → `packages/host` で解消（2026-09-02）。
  **ただし webview が橋に繋いで描く所は未確認**（画面のロック）
- **測ったのは「繋いでから絵が出るまで」で、操作から表示までではない**（Issue 005）
- **無作為の文字列は覗き見を防ぐだけで、暗号化はしていない。**同じ PC の中を流れる前提

### C34: AI が `PASS` / `FAIL` を出せる条件を、機械で見られる場合だけに絞る（2026-09-02）

`packages/core/src/run/steps.ts` / `case-runner.ts`。Issue 004 の「**AI が判断保留を出す条件**」。

**AI が自分で結果を決めてよいのは、期待結果が「」で囲まれた文字列 1 つで、それが画面の文字に
在るか無いかで決まるときだけ。**それ以外は全部 `BLOCKED`（判断保留）にして人へ渡す。

| 状況 | AI の結果 |
|---|---|
| 期待結果が `「X」と表示される` で、画面に `X` が在る | `PASS` |
| 同上で、画面に `X` が無い | `FAIL` |
| 期待結果に鉤括弧が 0 個（例: `ホーム画面が表示される`） | **`BLOCKED`** |
| 期待結果に鉤括弧が 2 個以上（どれを見るか決められない） | **`BLOCKED`** |
| 手順を操作へ落とせない（例: `メモを長押しする`） | **`BLOCKED`**（**端末に触らない**） |
| 非 ASCII の入力（端末の `input text` は IME を通らない） | **`BLOCKED`** |
| 操作の途中で落ちた・画面の文字が読めない | **`BLOCKED`** |

**Why 控えめに倒すか**: AI が推測で `PASS` を積むと、**人が見ていない `AUTO_PASS` が増えるだけ**で、
この製品の意味（誰が保証したかが証跡に残る・C1 / C17）が薄れる。**通ったことにするより、
判断を人へ渡すほうが安い。**

**推測しない。**「何も入力せずに保存をタップする」は、`保存` を取り出す推測をせず、
`何も入力せずに保存` という要素を探しに行って**見つからず `BLOCKED`** になる。
文を読み解いて別の要素を触ると、画面は動くので通ったように見え、**間違いに気づけない**
（`adapter-android` で要素の指定を完全一致だけにしたのと同じ理由・C31）。

**判定したことしか書かない。**`PASS` の注記は「画面の文字に「X」が在ることだけを見た」。
`「あいうえお」のメモだけが一覧に残る` のような**排他は確かめていない**ので、主張しない。

**この決定の弱いところ**

- **手順の語彙が 2 つしかない**（タップ / 入力）。スワイプ・長押し・キー入力は全部 `BLOCKED` になる。
  **足すのは、実際のシートで詰まってからにする**
- **画面の文字をどう取るかはアダプタ側**（`readScreenText`）。コアは形を知らない（C8）
- **この規則で実シートを流したのはまだテストの中だけ。**実機で 5 ケース通していない

---

### C35: 判定は 1 打鍵。取り消しは持たない（2026-09-02）

Issue 004 の「**人が押すキー**」。`packages/desktop/src/session/keys.ts` が割り当ての正本で、
**画面へ出す説明も同じ所から作る**（説明と実装がずれない）。

| キー | 置くもの |
|---|---|
| `v` | `VERIFIED` |
| `f` | `FAIL` |
| `b` | `BLOCKED` |
| `s` | `SKIP` |
| `Space` | 置かずに次へ（`AUTO_PASS` のまま） |

**Enter も修飾キーも要求しない。**要求すると、見ながら押す動作が 2 手になり、
「見た人がその場で置く」（C6）という前提が崩れる。
**逆に、修飾キー付きの打鍵は受け取らない。**⌘Q や ⌘R を奪うと、人が画面から出られなくなる。

**取り消し（`u`）は入れていない。**確定したケースをコア側で開け直す仕組みが要るので、
Issue 004 の「多くを作らないこと」に照らして見送った。**割り当てだけ先に作ると、
押しても何も起きないキーになる。**

**この決定の弱いところ**

- **押し間違いが直せない。**`v` を誤って押すと、人が見ていない行に `VERIFIED` が付く。
  直すには、その行を走らせ直すしかない。**取り消しは、実機で通した後に入れる**

---

### C36: 人が押すまで、次のケースへ進まない（2026-09-02）

Issue 004 の「**ケース間の遷移**」。`packages/host/src/run-session.ts`。

**時間で勝手に進めない。**進めると、人が見ようとしていたケースを見逃したまま
`AUTO_PASS` が積み上がる。**誰も見ない実行が要るなら、それは `mode: 'auto'`**
（`executeRun` を直に呼ぶ）の仕事で、混ぜない。

**遅れて届いた打鍵は捨てる。**打鍵にはケース番号が入っていて、待っている番号と違えば無視する。
**一番まずいのは、押した判定が別のケースに付くこと。**

**途中で終えたら、残りは `BLOCKED`。**画面を閉じられたときに「やっていない」ではなく
「判断保留のまま終わった」として残す。**走らせなかったことを「通った」にしない。**

---

### C37: 打鍵と実行状態は、映像と同じ橋で往復させる（2026-09-02）

`packages/live-bridge`。`GET <controlUrl>/events`（SSE で状態を流す）と
`POST <controlUrl>/input`（打鍵を受ける）を、映像と同じ token・同じ 127.0.0.1 の口に相乗りさせた。

**Why 相乗りか**: 口を 2 つに分けると、守り（127.0.0.1 限定・無作為の token・
localhost 以外へ繋がない）を 2 箇所で維持することになり、**片方だけ緩んでも気づけない。**

**形の正本は `@git-qa/core/session`。**橋は中身を知らずに運ぶので、**受け取る側が必ず検証する**
（`parseHumanInput` / `parseSessionState`）。`AUTO_PASS` は人の値として通らない（C17）。

**あわせて `executeRun` に、既に繋いだセッションを渡せる口を足した。**宿主は映像を出すために
先に繋いでいるので、実行器がそこで繋ぎ直すと**端末を二重に掴む。**渡されたセッションは
実行器が閉じない（持ち主が閉じる）。

**実装後に検査が捕まえたもの**

- **SSE のヘッダを flush していなかった。**書くものが無いうちは Node が溜めるので、
  publish が 1 度も無いと画面側の `fetch` が返らず、「繋がらない」ように見えていた

---

### C38: 画面の言語と外観は、色を決めずに切り替える（2026-09-02）

実物を触った人からの指摘（Issue 011 / 012）で入れたもの。**`DESIGN.md` が空のまま**なので、
**配色は決めていない**（§11）。

| 指摘 | 対応 | 色を決めていないか |
|---|---|---|
| メニューが英語 | 自前のメニューを Rust 側で組んだ。既定は日本語 | 文言だけ |
| 画面の文言が散っている | ja / en のカタログ 1 箇所に集めた。鍵の集合が揃っていることをテストで見る | 文言だけ |
| ライト / ダークが選べない | `color-scheme` と**ウィンドウの外観**を切り替える。**独自の配色は置かない** | OS の色を使う |
| プルダウンが古い | 3 つ並んだ押せる選択肢（`aria-pressed`）に変えた | 枠線は `currentColor` から作る |
| キーの見た目 | 1 文字のキーは正方形にして、キートップの厚みを付けた | `currentColor` のみ |

**タイトルバーは CSS では変わらない。**枠は OS が描くので、Tauri の `set_theme` へ伝える。
そのために **`@tauri-apps/api` を入れた**（§12）。

- **なぜ入れたか**: `invoke` を呼ぶ公式の口。既に `@tauri-apps/cli` を使っており、出どころは同じ
- **代替**: `window.__TAURI_INTERNALS__` を直に叩く。内部の口なので版が上がると壊れる。採らない
- **ブラウザで開いたときは黙って何もしない**（`pnpm --filter @git-qa/desktop dev`）。
  ここで落とすと、Tauri なしでは画面が出なくなる

---

### C39: 既存 OSS を採らずに作る。**自作するのは「結果の語彙」だけ**（2026-09-03）

Issue 010 の判定。**「独自性があるから作る」ではない。**

**どの製品も、人が見た PASS と誰も見ていない PASS を同じ値にする。**

| 製品 | 結果の語彙 |
|---|---|
| Playwright | `passed / failed / timedout / interrupted` |
| Maestro | `SUCCESS / ERROR` |
| Allure Report OSS | `status` ＋ 自由な `labels` |
| ReportPortal | `PASSED / FAILED / SKIPPED / INTERRUPTED / …` |
| Allure TestOps（商用） | `Passed / Failed / Skipped / Broken / Unknown` |

**ReportPortal だけが、この軸を部分的に持っていた。**`IssueEntity.auto_analyzed`（真偽値）で
「自動解析が付けた分類か、人が付けたか」を区別している。**この軸が実務で要ることの裏付け。**
ただし持っているのは**失敗の分類だけ**で、**成功側には無い**。

**自作しないもの**（既存に寄せる）

- 端末の操作と画面の取得 — `adb`（将来は scrcpy のサーバも候補）
- 見ながら触る体験 — scrcpy が示した形をそのまま採る
- 証跡の見せ方 — Allure / ReportPortal へ寄せられる可能性がある。**いま作らない**

**「既存へ貢献する」を採らなかった理由**: ReportPortal に「人が見た PASS」を足すのは、
**結果の語彙そのものを変える提案**になる。外部製品の中核データ構造を変える提案は通る見込みが薄く、
通っても見通しが立たない。**こちらの語彙を持ったまま、出力側で寄せるほうが現実的。**

**この決定を覆すべき条件**

- どこかの製品が「人が見た」を**結果の値**として持ったら、こちらの語彙を捨てて寄せる
- **主指標（手作業との所要時間比較・C7）で手作業に負けたら。**速くならないなら、
  語彙が正しくても作る理由が無い

---

---

## 未解決（事業判断が要る）

### D2: OSS として公開するか → **C15 で解決（2026-08-15）**

以下は判断に使った材料。**公開して差し支えのある要素は無い。**

ただし**掃除ができるのは今だけ**という制約がある。

- **commit history は後から消せない。**個人名・個人メールが入ると、公開時に history ごと作り直すことになる
- **サンプル・テストデータに実案件のものを使うと、後から差し替えても history に残る。**証跡には実顧客の画面が写るので、ここが一番危ない

**推奨**: **いま OSS と決める。**決めれば最初から公開前提の作法（検出 CI・ダミー案件でのサンプル）で作れる。`dbboard` / `md-business` と同じ `meta-taro` 配下の OSS 系列に並ぶ。

### history は公開直前に 1 回消せばよい（2026-08-14 決定）

「最新が動いていれば history は要らない」＝**公開時に squash して初回 commit 1 本にする。**本体リポについてはこれで足りるので、**開発中の commit を綺麗に保つ努力は不要。**

ただし効く範囲が分かれる。

| いつ | history 消し |
|---|---|
| **public に push する前** | **完全に効く。**好きなだけ書き換えてよい |
| **一度 public に push した後** | **効かない。**fork に残り、force-push 後も SHA 直指定で dangling commit が取れる |

**運用は「常に綺麗に保つ」ではなく「公開する瞬間までに 1 回掃除する」。**

**この方針が C10 を弱めるわけではない。**history を消せるのは**本体リポだけ**で、証跡リポには使えない（D3 参照）。混在させると、消せない側に消したいものが入る。

---

### D3: 証跡リポの粒度 → **分離自体は C16 で確定（2026-08-15）**

残っているのは粒度だけ。**顧客ごとに証跡リポを作るか、1 本にまとめるか。**

初期の企画草案 §11 は「顧客単位でリポを分ける」と書いている。公開との線引きは「顧客ごと」ではなく「**本体か証跡か**」で引くので（C16）、顧客単位に分けるかどうかは**公開の可否とは無関係な運用判断**になった。

**本体の作業は止まらない。**実案件で証跡を出す段（Phase 3）までに決めればよい。

---

### D4: 録画ありで運用したときの保管費の上限

Git LFS の課金。**常時発生する原価ではない**（C11 で設定になった）ので、「録画ありで運用したときの上限」だけ出せばよい。Issue 007。

---

### D5: 実案件のどれで主指標を測るか

手元の別プロジェクトで検証シートが動いているものから選ぶ。Issue 009（owner-task）。

---

### C40: 「アプリを起動する」を語彙に入れる。**行き先はシートに書いてあるものだけ**（2026-09-04）

2026-09-02 の実行記録（`runs/20260902-192058/run.json`）は **5 件とも `BLOCKED`** だった。
理由は端末側ではなく、**語彙にタップと入力しかなかったこと**。実物の検証シートは、
ほぼ必ず 1 行目が「アプリを起動する」で始まる。**どのシートを持ってきても 1 件目で止まる**状態だった。

- `アプリを起動する` → シートの見出し `# 対象:` が宣言したものを起動する
- `「com.example.app」を起動する` → 書いてあるものを起動する
- `設定を起動する` → **保留。**表示名からパッケージを当てにいかない

**当てにいかない理由。**「設定」がどのパッケージかは端末と地域で変わる。当てて別のアプリを
起動すると、画面は動くので通ったように見え、**間違いに気づけない**（`steps.ts` の元からの方針と同じ）。

端末側では `cmd package resolve-activity --brief` で在否を確かめてから `am start` する。
`monkey` は無いパッケージでも終了コード 0 を返すので使わない。

- **これを止めるべき条件**: シートの書き手が `# 対象:` にパッケージ名を書くことを嫌がるとき。
  そのときは「起動は人がやる」に倒す（保留になるだけで、証跡は壊れない）

---

### C41: 画面が追いつくのを待つ。**待つ長さではなく、結果を見て決める**（2026-09-04）

配っているシートを実機に当てたところ、5 件目（検索欄への入力）だけが落ちた。
**送った文字が黙って捨てられていた。**画面が切り替わった直後は、入力先がまだ受け取れる状態にない。

実測（エミュレータ・2026-09-04）: 0.2 秒 → 入らない / 0.5 秒 → 入らない / 1.0 秒 → 入る。

**固定の待ち時間で決め打ちしない。**端末が変わった瞬間に破れるため。3 つ入れた。

| 入れたもの | 何をするか |
|---|---|
| 操作のあとの間（既定 500ms） | 押した直後の画面は、まだ前の画面 |
| 要素が出るまで探し直す（既定 3 秒ぶん） | 1 回見て無いことを「無い」にしない。**待っても出なければ見つからないと言う** |
| 入力が入ったかを見て、**1 回だけ**送り直す | 入っていなければもう一度。**繰り返さない**（伏せ字の欄で何度も打ち込むことになる） |

**入らないまま終わっても落とさない。**入れても画面に出ない欄がある。入ったかどうかは、
このあと期待結果を見るところで判断される。

- **これを止めるべき条件**: 送り直しが二重入力を生む欄が実際に見つかったとき。
  そのときは送り直しを外し、待つ時間を端末ごとの設定にする

---

### C42: macOS では、使用目的を書き、署名を固定する（2026-09-04）

別プロダクト（warifu）で「許可のダイアログがたびたび出る」「カメラが動かない」という報告があり、
原因が 2 つに切り分けられた。**git-qa も同じ 2 つを踏んでいた。**

1. **`Info.plist` がそもそも無かった。**macOS は使用目的の記述が無いアプリに資源を渡さない。
   git-qa が踏む場所はファイル欄で、Finder 起動時にホームの下から検証シートを探すところ。
   **記述が無いと 0 件になる** — 「検証シートが選べない」が別の理由でまた起きていた
2. **署名が ad-hoc だった。**macOS は署名でアプリを見分けて許可を覚える。
   ad-hoc は建て直すたびに別物になるので、そのたびに聞き直される

書くのは**使うものだけ**（書類 / デスクトップ / ダウンロード）。カメラ・マイクは使わないので書かない。
ローカルネットワークも、待ち受けが 127.0.0.1 だけなので要らない。

**署名は人が行う**（§14 / §25 — 証明書の名前には個人名が入る）。手順は `docs/macos-packaging.md`。

- **これを止めるべき条件**: 待ち受けを 0.0.0.0 へ広げるとき。
  そのときは記述を足す前に、**なぜ広げるのかを決め直す**（C33 の前提が変わる）

---

### C43: 実機で分かった 3 つ。**黙って進めない**（2026-09-04）

実機（ASUS Zenfone `ASUS_AI2302` / Android 15 / 日本語）に当てて、エミュレータでは出なかった
壊れ方が 3 つ出た。**代役の端末で通ることは、実機で動くことの確認にならない。**

**1. 「アプリを起動する」の意味が端末によって違った**

`am start` だけだと、前に開いていた画面を再開する端末がある（実機がそうだった。
エミュレータは先頭に戻る）。**同じシートが端末によって違う所から始まる。**
いったん `am force-stop` してから起動するようにした。**起動は必ず同じ所から始まる。**

**2. 画面が消えたまま走ると、1 件目が `FAIL` になった**

消えている画面でも `uiautomator dump` は前の中身を返し、`am start` も静かに通る。
つまり**本当は失敗していないのに、失敗として証跡に残る** — いちばん悪い壊れ方。
操作と画面読みの前に、画面が点いていることを確かめるようにした。

**こちらから勝手に点けない。**点けても鍵が掛かっていれば同じ所で止まるうえ、
人の端末の状態を、頼まれていないのに変えることになる。

**3. 送った英数字が、端末の入力方式で変換された**

`input text wifi` を送ったら、端末には「うぃふぃ」が入った。日本語 IME を通っている。
**「端末の入力は IME を通らないので ASCII なら送れる」という前提が、実機で崩れた。**
しかも C41 の「入らなければ 1 回送り直す」が、変換されて入っていたことに気づかず
**二重に打ち込んだ**（「うぃふぃうぃふぃ」）。

送る前と後で入力欄の中身を見比べ、**空でも送った文字でもないものに変わっていたら、
送り直さずに理由を出す**ようにした。伏せ字の欄は責めない（中身が見えないのは当たり前）。

日本語表示の端末向けのシート（`sheets/android-settings-ja.tsv`）には、**文字入力の項目を入れていない。**
入れれば必ず止まるものを、通ることになっているシートに置かない。

- **これを止めるべき条件**: 端末の入力方式を git-qa 側で切り替えてよいと決めたとき
  （`adb shell ime set` で ASCII 専用の入力方式へ変えられる）。**人の端末の設定を変える**ことになるので、
  やるなら本人の許可を取る形にする

---

### C44: 保存は実行の一部。**置いた判定がどこへ行ったかを、人に見せる**（2026-09-04）

人が実機で 5 件の判定を置いた。**どこにも残らなかった。**

原因は 2 つ重なっていた。

1. **配布物（`.app`）は `--serve` で動くが、その経路に `run.json` を書く処理が無かった。**
   書いていたのは開発用の `pnpm app` 経路だけで、しかも**画面を閉じたあと**だった
2. 書けたとしても行き先が `/runs` になっていた。Finder 起動では作業ディレクトリが `/` で、
   macOS の `/` は read-only（`EROFS`）。**5 件置いたあとに保存で落ちる**

直したもの。

- **保存は `startRunSession` の一部**にした（`saveRun`）。置き終わった時点で書く。
  画面を閉じるまで待たない
- 書けない場所だったときは、**人が開けて、そのまま人へ渡せる所**へ逃がす
  （`~/Documents/git-qa/runs`）。`~/Library/Application Support` は仕様どおりだが、
  証跡は人が探して添付するものなので採らない
- **書いた場所と、書けなかった理由を画面に出す。**出さないと、保存されたのかどうかが
  分からないまま終わる。**保存に失敗しても、置いた判定そのものは失わない**

**この製品は証跡を出すための道具である。**証跡が出ない経路を配ったのは、
機能が欠けていたのではなく、**製品が存在していなかったのと同じ。**

- **これを止めるべき条件**: 証跡の置き場を人が指定したいと言ったとき。
  そのときは画面から選べるようにする（いまは決め打ち）

---

### C45: ハンドルの規則は入口で見る。**終わってから落とさない**（2026-09-04）

人が実機で 5 件の判定を置いた。**2 回とも消えた。**

2 回目の原因は、証跡の schema が担当者ハンドルを ASCII に限っていたこと。

```
/operator/handle must match pattern "^[A-Za-z0-9][A-Za-z0-9-]{0,38}$"
```

入力されたのは「めたたろ」。**5 件置き終わったあとの保存で落ちた。**

- 規則は **schema から読む**（`run/handle.ts`）。書き写すと、片方だけ直したときに
  **入口は通るのに保存で落ちる**という形で壊れる
- **入口サーバと画面の両方で見る。**画面には「何が書けるか」を出し、合わないと押せない
- 画面（webview）から使えるよう、`@git-qa/core/session` から出す
  （`@git-qa/core` の本体は Node 専用の口を含むので、画面のビルドが壊れる）

**いちばん高くつく壊れ方は、全部終わってから落ちること。**入口で止める。

- **これを止めるべき条件**: 日本語のハンドルを使いたいと決めたとき。
  そのときは schema（正本）を先に変える。**画面側だけ緩めない**

> **2026-09-06、この条件が満たされた。C53 を見ること。**

---

### C46: AI が自分の画面を見られるようにする（2026-09-04）

**これが無いあいだ、AI は「右のカラムに何が出ていますか」と人へ 2 度聞いた。**
そのあいだに人の作業が消えた。**人に目の代わりをさせない。**

`app_screenshot`（MCP）を足した。端末の画面を撮る `device_screenshot` とは別物で、
**この道具自身の窓**を撮る。

- `mode: window` — 窓だけ。**手前に重なった別アプリは写らない**
- `mode: screen` — 画面全体

**どちらも要るのは画面収録の許可だけ**（2026-09-04 夕に訂正。下の C47 を見ること）。

**アプリ名をそのまま埋め込まない**（引用符を閉じられると別の命令になる・§21）。
撮った絵は読んだ時点で消す（人の画面が temp に溜まり続けるのは、頼まれていない）。

- **これを止めるべき条件**: 画面全体を撮ることを人が望まないとき。そのときは window だけに絞る

### C47: 窓は「画面の四角」ではなく「窓の番号」で撮る（2026-09-04・**C46 の訂正**）

C46 は窓の位置を AppleScript で聞き、`screencapture -R x,y,w,h` で切っていた。
**`-R` が撮るのは画面のその四角であって、窓ではない。**実測（2026-09-04 17:14）:
git-qa の絵として返ってきた PNG の上半分が、**手前に重なっていた別プロジェクトのアプリの窓**
だった。エラーは出ない。**C46 で塞いだ「黙って違う絵を返す」壊れ方が、別の入口で再発していた。**

`screencapture -l <CGWindowID>`（窓そのもの）へ替えた。窓の番号は
`CGWindowListCopyWindowInfo` を `osascript -l JavaScript` の `ObjC.bindFunction` 経由で聞く
（`CGPreflightScreenCaptureAccess` と同じ手）。同じ絵を撮り直し、**別アプリが消えて
git-qa の窓だけが返ることを確認した。**

**副産物として、window モードのアクセシビリティ許可が要らなくなった。**位置を聞くのに
`System Events`（補助アクセス）を使っていたのが理由で、`kCGWindowOwnerName` は画面収録の
許可の範囲で読める。C46 に書いた「window にはアクセシビリティの許可が要る」は**もう成り立たない。**

**アプリ名は `JSON.stringify` で JS の文字列リテラルとして閉じる**（§21）。
以前の英数字ホワイトリストと違い、`ターミナル` のような名前も落とさずに渡せる。

- **これを止めるべき条件**: 別の Space・畳んだ窓で `-l` が撮れないことが分かったとき。
  そのときは「撮れなかった」と言って落ちる（**四角で切る形には戻さない**）

### C48: 主指標（Issue 008）の合格基準と測り方を先に決める（2026-09-04）

Issue 008 は「**基準を先に決めてから測る**」を作業手順の 1 番目に置いている。数字を見てから
基準を決めると、**出た数字に合わせて基準が動く。**測る前にここへ書く。

## 決めたこと

**合格の線**（同じケース群・同じ端末・同じ人）

| 道具の所要時間 ÷ 手作業の所要時間 | 判定 |
|---|---|
| **0.70 以下**（30% 以上の短縮） | **合格。**MVP の主指標を満たしたとする |
| 0.70 超〜1.00 | **不合格。**短くはなっているが「明確」ではない。ケース単位の内訳から一番食っている所を特定し、Issue にする |
| 1.00 超 | **不合格。**道具のほうが遅い。作り直しの範囲を決め直す |

**測り方**

1. **同じ人が両方をやる**（人によるばらつきを消す。n=1 で比べる以上、ここを揃えないと差が読めない）
2. **手作業を先にやる。**道具を触った直後に手作業をやると、手作業が遅く出て**道具に有利な数字**になる
3. **同じ日・同じ端末・同じケース群**でやる
4. 時間は**止めずに測る**（wall clock）。**AI の思考時間・端末の起動・adb の接続も人の待ち時間**なので外さない
5. **初回で遅いなら、初回で遅いと書く。**「慣れれば速くなる」で割り引かない
6. 手数は**道具側だけ**を実測する（`run.json` から自動で数える）

## 根拠

- **0.70**: 20 ケースで 60 分の作業なら 18 分減る。人が「楽になった」と体感する幅であり、
  1 回だけの測定に乗るばらつき（同じ人が同じ作業を繰り返しても ±10〜15% は動く）より外側にある。
  10% 減を合格にすると、**ばらつきと区別が付かない**
- **手数を道具側だけにする**理由: 手作業側の打鍵・マウス操作を同じ精度で数える方法が無い。
  数え方の違うものを並べると、**比べたことにならない。**手数は主指標ではなく、
  差が出なかったときに**どこで食っているかを切り分けるための内訳**として使う

## 採らなかった案とその理由

- **50% 減を合格にする** — 厳しすぎる。人が判定を置く時間は道具でも減らない（減らしてはいけない）。
  20 ケースぶんの「見て決める」時間が残る以上、半減は構造的に届かない可能性が高く、
  **届かない線を引くと、線のほうを後で下げることになる**
- **主観評価（楽になったか聞く）を併用する** — 作った側が聞くと甘く出る。§27 の通り、
  要望は使った人が自分で書く。**数字と要望を混ぜない**
- **複数回測って平均する** — 望ましいが、実案件を 2 回止めることになる。
  まず 1 回で線を跨ぐかを見て、**跨がなかったときに回数を増やす**

## これを止めるべき条件

- 手作業の所要時間が**そもそも記録されていない**案件しか無かったとき（比較対象が作れない）
- 対象ケースが 20 件に満たないとき（1 ケースの当たり外れが全体を動かす）

## 実行状況

**測っていない。**前提の Issue 009（実案件と 20 ケースを決める・**担当は人間**）が未完了。

**さらに、009 の完了条件「手作業の所要時間は道具を作る前に測っておく」は、既に窓が過ぎている。**
道具は動いてしまっており、測る人は道具を知っている。**この汚染は消せない。**
測る場合は、**数字に「手作業側は道具を知っている人が実施」と注記を付ける**（消せないものを
無かったことにしない）。注記の付いた数字は、**道具に不利な方向には歪まない**ので、
合格が出たときの根拠としては使える。**不合格が出たときに「汚染のせいだ」と言わない。**

### C49: 判定を置いた打鍵の「回数」だけを証跡に残す（2026-09-04）

C48 で「手数は道具側だけ実測する（`run.json` から自動で数える）」と決めたが、**数える対象が
証跡に無かった。**端末を触った回数は `humanActions` の長さで出るのに、**判定を置いた打鍵は
1 つも残っていない。**

`run.json` の各ケースに `humanInputs: { verdict, advance }` を足した。

- **回数だけ。**どの判定をいつ置き直したかの履歴は残さない。`run-session.ts` に
  「置き直した人と時刻は最後の 1 回のもの。途中経過は残さない（**残すなら形を別に決める**）」
  と書いてあった通り、**別の形として回数だけを決めた**
- **受け取った打鍵だけを数える。**まだ走っていないケースへの打鍵は捨てているので、数えない
  （捨てた分まで載ると、人が押していない手数が計上される）
- **打鍵の無かったケースには付けない。**0 を並べると、**打って 0 回**と**まだ実施していない**が
  同じ形になる（`humanActions` と同じ扱い）
- 正本は **JSON Schema**（`packages/core/schema/run.schema.json`）。TypeScript の型はその写し

`verdict` が 2 以上のケースは置き直したケース。**多く出た所は、画面が分かりにくい所。**

- **これを止めるべき条件**: 回数だけでは足りず「何回目で何に変えたか」が要ると分かったとき。
  そのときは履歴の形を決めてから足す（**回数を履歴で置き換えない。**両方要る）

### C50: 映像が落ちたら、黙らずに繋ぎ直す（上限つき・2026-09-04）

**実物を使った人の報告**: 「検証中にケーブルが抜けたら、再度さしても再開できないです」（Issue 014）。

映像が落ちた理由を控える口（`onLiveError`）はあったが、**繋ぎ直しに来る者が居なかった。**
橋の中身が尽き、画面は最後の 1 枚で固まる。挿し直しても誰も繋ぎに行かない。

## 決めたこと

**人が押すのではなく、自動で繋ぎ直す。**`live-session.ts` が 2 秒ごとに映像の口を開け直し、
**上限 5 分**で諦める。待っている間は理由に「繋ぎ直しています」を添えて画面へ出し、
**戻ったら理由を消す。**

あわせて、Android アダプタが 1 枚も受け取れなかったとき、**まず `adb devices` を見る。**
端末が居なければ「ケーブルが抜けていないかを見て、挿し直すこと」と言う。

## 根拠

- **検証中、人の手は端末の側にある。**画面のボタンを探させると、その分だけ検証が止まる
- **2 秒**: 人がケーブルを挿し直すより短い。長くすると、挿したのに戻らない時間ができる
- **5 分**: 抜けたことに気づいて挿し直すには足りる。**無限にしない** ——
  「繋ぎ直しを繰り返すと、黙ったまま回り続ける」は、この道具が既に 1 度踏んだ穴
- **戻ったら理由を消す**: 出したままだと、直っているのに直っていないように見える

## 採らなかった案とその理由

- **画面に［繋ぎ直す］ボタンを出す** — 人が押すまで戻らない。**抜けたことに気づいていない
  ときに、何も起きない。**なお自動で戻らなかったときの手動の口は、まだ無い（残った穴）
- **無限に繋ぎ直す** — 端末が壊れた・別の PC に挿された場合に、黙って回り続ける
- **画面側（`stream.ts`）で繋ぎ直す** — 橋の URL を握り直す話になり、Node 側で映像の口を
  開けたままにするほうが小さい。**橋そのものが切れた場合は塞げていない**（残った穴）

## これを止めるべき条件

- 5 分の間に端末が別の serial で戻ってくることが分かったとき（挿し直しで serial が変わる端末）。
  そのときは serial の突き合わせ方から決め直す
- 繋ぎ直しの最中に `screenrecord` が端末側で溜まることが分かったとき

## 実行状況

**TDD で実装済み**（RED 5 件 → GREEN・`pnpm verify` 740 passed）。
**実機で抜き差しして確かめていない。**合否は人が実物で記入する（§19）。

### C51: 人が読む文に、道具の内部名を出さない（2026-09-04）

**実物を使った人が画面で見た文**:

> ライブ映像を出せない: [android] screenrecord が映像を 1 枚も返さずに終わった

この 1 文に、人には要らないものが 3 つ入っていた。

- `[android]` — 複数の相手に繋ぐときログで切り分けるための印。**画面では邪魔**
- `screenrecord` — Android の内部コマンド名。**使う人はこの名前を知らない**
- `ライブ映像を出せない:` — 出ている場所が「ライブビュー」の欄なので、**見出しの繰り返し**

そして肝心の「**何をすればいいか**」が無い。実際、この文が出た後で端末を調べたところ、
端末は繋がっており、同じ引数で `screenrecord` は **3 秒で 155,024 バイト**返した。
**文からは原因が絞れなかった。**

## 決めたこと

画面に出す文は、この順で書く。

1. **何が起きたかを、人の言葉で先に**書く（道具の内部名を先頭に置かない）
2. **次に何をすればいいか**を書く
3. 技術的な詳細（コマンド名・`stderr`・serial）は**括弧で後ろに残す。消さない**
4. **見出しと重ねない。**欄の中に出る文は、欄の名前を繰り返さない

`[android]` のような対象の印は**消さない。**ログでは要る。
**画面へ出す直前に落とす**（`humanMessage()`・`packages/core/src/adapter/errors.ts`）。

## 直した文（Android アダプタ・11 件）

| 前 | 後 |
|---|---|
| `screenrecord が映像を 1 枚も返さずに終わった` | `端末の画面を受け取れなかった。ほかに git-qa が動いていたら閉じて、もう一度始める（端末が画面を配れるのは 1 つの実行だけ）` |
| `端末の画面が消えている。点けてから繋ぎ直すこと` | `端末の画面が消えている。画面を点けて、ロックを解除する` |
| `adb <args> が失敗した: <stderr>` | `端末とやりとりできなかった: <stderr>（adb <args>）` |
| `繋げる端末が無い（<seen>）` | `繋がっている端末が無い。USB で繋ぐか、エミュレータを起動する（adb には <seen> と見えている）` |
| `セッションは閉じられている` | `端末との接続はもう閉じている` |
| `screencap が空を返した` | `端末の画面を撮れなかった（screencap が空を返した）` |

## 根拠

**「1 枚も返さずに終わった」で分かるのは、書いた人だけ。**この道具は、コードを書いていない
人が実物を見て判定を置くためのもの。**読んで動けない文は、出していないのと同じ。**

技術的な詳細を消さないのは、**後から原因を絞るのに要る**から。前に置くか後ろに置くかの話であって、
捨てる話ではない。

## 採らなかった案とその理由

- **`[android]` を `AdapterError` から消す** — ログで切り分けられなくなる。
  複数の相手に繋ぐ想定がある以上、**印そのものは要る**
- **文言を英語のままにして訳を足す** — 出どころが 2 つになる。文言の正本は
  `packages/desktop/src/i18n/messages.ts` に寄せてある

## これを止めるべき条件

- 括弧の中の詳細が長すぎて、**人が読む部分が埋もれる**と分かったとき。
  そのときは詳細をログだけに回し、画面には出さない形を決める

## 実行状況

**11 件を書き直し、`humanMessage()` を入れた**（`pnpm verify` 744 passed）。
**実物の画面で読んだ確認は、そちらの記入待ち。**

### C52: 自動テストモードを作る。人の判定は残す（**方針・人が出した**・2026-09-06）

**この方針は人が出したもの。**AI が決めたのではない。

> git-qa は人が責任を負うためにテストを現在は人の操作としていますが、自動テストモードも
> 実装します。将来的にはほぼ人の手を離れるかです。画像や、短い動画などが保存されるように
> なるので、あとで問題ある部分を見直せればよい時代になるはすです。

## 何が変わるか

これまでの前提は「**人が実物を見て判定を置く**」で、C1 / C17 はその上に立っている。
そこへ「**人が見ないまま走り切る道**」が加わる。**判定を置く道が消えるのではなく、
2 本になる。**

`run.json` は既にこの 2 本を区別できる形になっている。

| 値 | 意味 |
|---|---|
| `VERIFIED` | 人が見て置いた |
| `AUTO_PASS` | 人は見ていない。AI の判定のまま |

**この 2 つを 1 つに潰さない**という決めごと（`run.schema.json` にも書いてある）は、
自動テストモードが入った後で**いっそう効く。**後から「これは誰が見たのか」を分けられる。

## 人の目が後ろへ回るということ

人が横で見ないなら、**見る材料が残っていないと後から確かめられない。**
方針の言う「画像や短い動画が保存される」は、証跡の側の要求になる。

- ケースごとの録画は `CaseRecording` として形が決まっている
  （`recorded` / `not_requested` / `failed` / `unsupported` を潰さない・C11）
- **`failed` を握り潰さないのは、この方針の下でより重い。**録画が落ちていたことに
  後から気づいても、そのケースはもう再現できない

## いま決めない（決める番になったら決める）

- 何を、どの粒度で残すか（毎ケース録画か、落ちたケースだけか）
- 保管の量と期限（Issue 007「証跡の保管コスト」がこれ）
- 自動で走らせた結果を、人がどこで見直すか（見直す画面がまだ無い）

## これを止めるべき条件

- `AUTO_PASS` が積み上がったまま**誰も見直さない**運用になったとき。
  そのときは「人が見ていない結果」を出荷判断に使わせない形を先に決める


---

## 企画側側で先に潰す作業

- ~~D2（OSS 公開の可否）~~ → **C15 で確定（2026-08-15・公開する）**
- ~~D3（本体と証跡のリポ分離）~~ → **C16 で確定。**残るのは粒度だけで、Phase 3 まで決めなくてよい
- **リポ作成時に `meta-taro` 配下・public で作る。**個人名検出 CI を初回 commit から入れる（Issue 003）

### 実装リポ側で行う（企画側は先回りしない）

- Windows→iOS のライブビュー実測（Issue 001）
- Adapter 境界の定義（Issue 002）
- デスクトップ骨格の技術選定（Issue 003）
- 既存 OSS の実地調査と「作らない」判定（Issue 010）
- Git LFS の試算（Issue 007）

### C53: ハンドルは日本語を通す（2026-09-06・**人が出した指示**）

人が実物の前で 2 度止まった。2 度目に、こう言った。

> いやです。ただちに修正しなさい。

**止めていたのは規則のほうだった。**AI は「`metataro` と打ち直せば押せます」と案内したが、
それは**人に道具の都合を飲ませているだけ**だった。C45 が「これを止めるべき条件: 日本語の
ハンドルを使いたいと決めたとき」と書いていた、その条件そのもの。

## 決めたこと

**schema（正本）の `handle` を変える。**C45 の指示どおり、**画面側だけ緩めない。**

| | 前 | 後 |
|---|---|---|
| 規則 | `^[A-Za-z0-9][A-Za-z0-9-]{0,38}$` | `^[^\s./\u0000-\u001f\\-][^\s/\u0000-\u001f\\]{0,38}$` |
| 日本語 | 通らない | **通る** |
| 空白 | 通らない | 通らない |
| `/` `\`・制御文字 | 通らない | 通らない |
| 先頭の `.` `-` | 通らない | 通らない |
| 長さ | 39 まで | 39 まで |

## 根拠

- **ハンドルはどこにも経路として使っていない。**`run.json` の `verifiedBy` / `operator.handle`
  に入り、画面に出るだけ。置き場所の名前は `runId` と case 番号から作る（別の `$defs`）。
  **日本語を通しても壊れる所が無い**
- `runs/` は Git に入れないので、**public リポジトリに個人が残る話にもならない**（§25）
- 空白と区切りを残して弾くのは、**いま使っていなくても、使われたときに事故る形を
  最初から入れない**ため（§21）

## 採らなかった案とその理由

- **人に英数字で打ってもらう** — 道具の都合を人に飲ませている。**そのために検証が止まった**
- **画面側だけ通す** — C45 が名指しで禁じている。**5 件置き終わったあとの保存で落ちる**
  形に戻る（実際に 2 回起きた）
- **規則を全部外す** — 空白や `/` は、置き場所の名前に使われた瞬間に事故る

## これを止めるべき条件

- ハンドルを**ファイル名やディレクトリ名に使う**と決めたとき。
  そのときは規則ではなく、**使う側で安全な名前へ変換する**（規則を戻さない）

## 実行状況

**schema を変え、画面の文言も合わせた**（`pnpm verify` 751 passed）。
**実物で確認済み**: `めたたろ` のまま「検証を開始する」が押せる状態になった。

### C54: ウェブ検証は、人が持っている Chrome を CDP で動かす（2026-09-06）

**人が「直近の本命はウェブ」と言った**（Issue 015）。roadmap は Playwright headed を
想定していたが、決める前に測ったら、**依存を 1 つも足さずに作れることが分かった。**

```
node v22.23.2 / WebSocket: function     ← Node に WebSocket が入っている
/Applications/Google Chrome.app         ← ブラウザは既にある
```

## 決めたこと

**新しい npm 依存を足さない。**人が既に持っているブラウザを
`--remote-debugging-port` で起こし、**CDP（Chrome DevTools Protocol）を直接叩く。**

| | 使うもの |
|---|---|
| 画面を運ぶ | `Page.startScreencast`（JPEG フレーム） |
| 触る | `Input.dispatchMouseEvent` / `Input.insertText` |
| 画面の状態 | `Runtime.evaluate`（DOM。**アクセシビリティツリーへ潰さない**・C24） |
| 行き先 | `Page.navigate`（シートの `# 対象:` が宣言した URL だけ・C40） |

**映像はアプリの枠の中に出す**（C4 / C27 の方式 A）。別窓のブラウザを見せる形は採らない。
そのために `LiveViewTransport` に**画像フレームを運ぶ種類を足す**（H.264 とは別の道）。

**ブラウザは `adb` と同じ扱いの外部前提**にする。無ければ、無いと言って止まる。

## 根拠

- **依存はゼロで足りる。**Playwright は成熟しているが、ブラウザの別途取得（約 150 MB）が
  要り、`onlyBuiltDependencies` の方針とも噛み合わせが要る。**足さずに済むなら足さない**（§12）
- **人が判定する相手は、人が使っているブラウザであるべき。**「動く／動かない」を見るのに、
  検証専用に落としてきた別のブラウザを使う理由が無い
- `Target.kind: 'web'` も `ObservationKind: 'dom'` も**最初から空けてある**（C12）。
  設計を変える話ではなく、埋める話

## 採らなかった案とその理由

- **Playwright headed（別窓のブラウザを人が見る）** — roadmap の想定。
  **1 画面で見て 1 打鍵で置く**という形が崩れる（C4）。人が窓を 2 つ見比べることになる
- **headless で動かす** — 「動く／動かない」を見る道具が、**人が見ない描画で判断する**のは筋が悪い
- **既存の H.264 の道に載せる** — ブラウザから H.264 を出すには符号化が要る。
  JPEG フレームのほうが**画面側の復号も簡単**（`createImageBitmap` で済む）

## これを止めるべき条件

- **CDP を自分で書くのが割に合わなくなったとき**（対応するイベントが増えすぎる、
  ブラウザの版差で壊れる）。そのときは Playwright へ移す。**この決定に固執しない**
- 人が「ブラウザは検証専用に固定したい」と言ったとき

## 実行状況

**着手した。**Issue 015 に段取りを書いた。

### C55: デスクトップは「触れ方」を段で持つ。アプリの作りで線を引かない（2026-09-06）

**人の指摘**:

> タウリですが、はなからしぼってきめないでほしいですが。どんなものでもできてほしいので。

AI は「アクセシビリティに名前が出るアプリなら作れます、出ないなら別途相談です」と
線を引こうとした。**それはこちらの都合。**引き受ける相手を、こちらの実装しやすさで
決めていた。

## 決めたこと

**どのアプリでも成立する道を持つ。**触れ方を 1 つに賭けず、**段**にする。
上から順に試し、**どの段で判断したかを証跡に残す。**

| 段 | 見る／触る手 | 実測（2026-09-06・このマシン） | 効く相手 |
|---|---|---|---|
| 1 | **アクセシビリティ（AX）** | **793 ms** で名前付きの要素が取れた | Tauri / Electron / Cocoa / SwiftUI |
| 2 | **画面の絵から文字を読む（OCR）** | **3,607 ms** / 44 行 / 座標つき | **どんなアプリでも**（自分で描く系を含む） |
| 3 | 座標 | —— | 最後の手段。**シートが壊れやすいと明示する** |

**見るほう（映像）は段が要らない。**窓を撮るのはどのアプリでも同じ:
**118 ms / 枚（約 8.5 fps）・737 KB / 枚**。今日作った `image-frames` の道にそのまま乗る。

## 依存はまた足さない（C54 と同じ）

- 窓を撮る: `screencapture -l <窓ID>`（OS 付属）
- 要素を読む: `osascript`（OS 付属）
- 文字を読む: **OS の Vision**。`xcrun swiftc` で 37 KB の小さな実行ファイルを 1 つ作るだけ
  （このマシンで実際に作って動かした）

## 根拠

- **「どんなものでもできてほしい」が要求。**段にすれば、AX が空でも OCR で降りられる
- **OCR の読み違いは、安全な方向に外れる。**読み違えると期待した文字が「見つからない」に
  なり、`BLOCKED` か `FAIL` として人の目に回る。**通ってはいけないものが通る側には倒れない**
  （実測で `書きました` を `響きました` と読んだ。密な端末の文字での話で、
  ボタンやラベルではもっと当たるが、**外れる前提で設計する**）
- **どの段で判断したかを証跡に残す。**「AX で名前を見た」と「絵から読んだ」は、
  後から見返すときに重みが違う。**潰さない**（`VERIFIED` と `AUTO_PASS` を潰さないのと同じ）

## 採らなかった案とその理由

- **アプリの作りを先に聞いて、対応可否を決める** — 引き受ける相手をこちらの都合で狭める。
  **人が使いたい相手が先にある**
- **OCR だけにする** — AX で名前が取れるなら、そちらが速くて確か（793 ms 対 3,607 ms）。
  安いほうを先に試さない理由が無い
- **画像のテンプレート照合** — 見た目が少し変わるだけで壊れる。OCR のほうが変化に強い

## これを止めるべき条件

- OCR の読み違いが**人の手を止めるほど多い**と分かったとき。
  そのときは段 2 の使い方（完全一致をやめる・候補を人へ見せる）から決め直す
- Windows で試す番になったとき。**UI Automation は API が別物**なので、
  段 1 の中身は作り直しになる（段の考え方は変わらない）

## 実行状況

**測っただけ。まだ実装していない。**

### C56: 「１％未満」を切り捨てない。それがこの道具の主張（**人が出した**・2026-09-06）

**人の言葉のまま。**

> 大企業が、chrome, エッヂにしぼったサポートをしはじめたのは、それはコストです。
> firefox は近年だめだめだったから補償コストがあがっていました。
> でも今の AI ドリブンであれば、それは話が変わります。
> AI ドリブンな開発言語、AI ドリブンなパソコンが誕生し、根底から世界は一変するでしょう。
> が、ゆえに、こういった小さくも AI の力で大きくなりうる１％未満は
> AI の力で対応するべきとなりました。

> セキュリティ企業などのブラウザに対応していると書くだけで、大企業がこの OSS を意識するのです。

> とうぜんはったりは NG ですが

## 何が決まったか

**対応の広さは「あとで足す機能」ではない。この道具が何のためにあるかの説明そのもの。**

検証の対応範囲が Chrome / Edge に狭まったのは、技術的な理由ではなく**人手のコスト**だった。
1 つ足すたびに、書く人・直す人・確かめる人が要る。**１％未満のために誰も出せなかった。**

**そのコストの形が変わるなら、切り捨てる理由も無くなる。**

## これが効く所

- **対応表を後ろに置かない。**README と案内の前のほうに出す。
  「何で検証できるか」がこの道具の看板になる
- **「その他（場所を指定）」のような逃げ道を、例外ではなく設計として持つ。**
  名前を数え上げに行かない —— 数えきれないし、増える
- **Firefox / Safari は「宣伝のため」ではなく「主張の実証」。**
  1 つでも落ちていると、主張そのものが弱くなる

## 同時に決めたこと —— **はったりを書かない**

人が名指しで釘を刺している（「とうぜんはったりは NG」）。

**対応表に書けるのは、動かして確かめた分だけ。**

- 「動く見込み」と「動いた」を、表の上で混ぜない
- 確かめていないものは**確かめていないと書く**
- **1 度でも「書いてあるのに動かない」が出たら、この主張は死ぬ**

## これを止めるべき条件

- 広げることで、**核（人が実物を見て判定を置く）が薄くなった**とき。
  対応表の行数は、`VERIFIED` の重みより先に来ない

### C57: 押す前に前面へ出す。Electron には「中身を出して」と頼む（2026-09-07・**実測で分かった**）

**決めたこと**

1. **押す直前に、対象のアプリを前面へ出す。**出たことを確かめてから押す。
2. **繋いだ直後に `AXManualAccessibility` を立てる。**失敗しても進む。

**根拠**

Electron で作られたデスクトップアプリを macOS で読ませたときに、2 つ踏んだ。

**(1) 押す座標は画面全体の座標なので、隠れていれば手前の別アプリが受け取る。**

撮るほうは 2026-09-04 に直してある（`screencapture -l <窓番号>` なので、重なっても目的の窓だけが写る）。
**押すほうが直っていなかった。**実際にこうなった。

```
click at {1648, 300} → Google Chrome のツールバーのボタン
click at {1400, 237} → warifu の窓
```

**映像にはそのデスクトップアプリが写ったまま、押した先だけが別のアプリ**になる。
検証の結果としては最悪の形で、PASS でも FAIL でもないものが記録される。

窓の一覧で「手前かどうか」を見るだけでは足りなかった。押した拍子に並び順が変わるうえ、
**System Events の当たり判定と窓の一覧が、いつも同じ答えを返すわけではなかった。**

2026-09-06 に「操作のたびに前面へ出すな」と言われている
（「チャット欄に書き込もうとしてフォーカス当てると、実際のアプリに移動しちゃうね」）。
**その指摘は見る側には今も効かせている。**押す側だけ戻した。
**奪わないことより、別のアプリを押さないことが先。**

**(2) Electron は、聞かれるまで中身を出さない。**

出していない間はこうなる。

- 段 1（AX）は `group` が 11 個だけで、**文字が 1 つも取れない**
- **押すと `-25211`（補助アクセスは許可されません）。**
  **許可はあるのに、その文言で返る。**原因の見当がまるで違う方へ向く

`AXManualAccessibility` を立てた瞬間に木が生えて、`radio button 一覧` として押せた。

**採らなかった案とその理由**

- **押す前に毎回 `ui_elements` を読んで木を起こす** — 副作用で起きているだけで、当てにできない。
  1 時間つまずいた原因がこれ（読んだ直後だけ押せる）
- **Electron を特別扱いする** — C55 の「アプリの作りで線を引かない」に反する。
  この属性は**全部の相手に立てにいって、駄目なら黙って進む**（普通の Mac アプリでは必ず失敗するが、
  そちらは最初から中身が見えている）

**これを止めるべき条件**

- 押すたびに前面へ出るのが、人の作業を邪魔して**使われなくなった**とき。
  そのときは「押す前に人へ確かめる」か「人が前面に出すまで待つ」へ変える。
  **黙って隠れたまま押す形には戻さない。**

**実行状況**

実装済み・検査済み（`packages/adapter-desktop/test/on-top.test.ts` / `manual-ax.test.ts`）。
そのデスクトップアプリの検証シート 6 件が、この 2 つを入れたあとに通った（**下書き。人はまだ置いていない**）。

**追記（同日・**前面に出さずに押す道を試して、駄目だった**）**

人にこう言われた。

> 操作したときにそのデスクトップアプリがいちいち一番うえにくるのはふせげないの？
> うーん、おしいw一瞬上にきて引っ込むねw

前面に出すのは、**画面の座標へ押しているから**必要になる。宛先をアプリにできれば要らない。
`CGEventPostToPid`（指定したプロセスにだけ出来事を届ける）を試した。

**Electron には届かなかった。**

- 呼び出しはエラーを返さない。`ok` が返る。**それでも画面は動かない**
- 前面に出していても届かない
- `kCGMouseEventClickState` を 1 にしても届かない

**一度「動いた」と報告してしまった。**画面の読み違いで、実際には前の状態を見ていた。
**エラーが返らない道は、返り値では確かめられない。**画面が変わったことで確かめる。

`AXUIElementCopyElementAtPosition` ＋ `AXPress` も試したが、JXA の ObjC 橋で
`-25201`（引数が不正）から先へ進めなかった。**持ち帰る。**

**いまは `System Events` の `click at` に戻してある。**
**人には「一瞬上に来て引っ込む」と見える。直っていない。**

**残っている宿題**

- 押すたびに相手が一瞬前面へ出る。**指摘はまだ塞げていない**
- 次に試すのは AX の要素を直接押す道（`AXPress`）。前面も並び順も要らず、
  **隠れたままでも押せる**。JXA から呼べなかったので、呼び方を変えて試す
  （Swift の小さな実行ファイルにする手もある。OCR で同じことをしている）

**追記（同日・**前面に出さずに押せるようになった**）**

人の判断で Rust の小さな実行ファイルを 1 つ足した（`packages/adapter-desktop/tools/input`）。
**依存は 0。**OS のフレームワークへ直接繋いでいる（`git-qa-ocr` と同じ考え方）。

**なぜ Swift ではなく Rust か。**人にこう聞かれた。

> win版でどうなる？

Swift で書くと **Windows へ 1 行も持っていけない。**しかも**そのデスクトップアプリは Windows 前提のアプリ**で、
そちらが本命の相手になる。Rust は Tauri を建てるのに**もう前提に入っている**ので、
道具立てを増やさずに両方の OS を書ける。

**切り分けの結果（同じ道を二度試さないために残す）**

| 試したこと | 結果 |
|---|---|
| `CGEventPostToPid`（JXA から） | **届かない。**`ok` が返るのに画面は動かない |
| `CGEventPostToPid`（Rust から・HID の source・clickState 付き） | **届かない。**前面に出していても同じ |
| **`AXUIElementPerformAction` / `AXPress`（Rust から）** | **効いた。前面に出さずに押せた** |

**JXA の橋のせいではなく、Chromium / Electron が `CGEventPostToPid` を受け取らない。**

**実測（そのデスクトップアプリ・macOS）**

| | 1 押し | 相手が前面に出るか |
|---|---|---|
| 最初（look を 2 回・OCR 込み） | **3 秒超** | 出る |
| 1 本の script にまとめた後 | 520 ms | 出る |
| **`git-qa-input`（AXPress）** | **99〜168 ms** | **出ない** |

**押せない場所には落とす。**絵で描かれているだけの所には押せる部品が無いので、
そのときだけ前から在る道（前面へ出して画面の座標を押す）を通る。**黙って何もしないことはない。**

**まだ残っていること**

- **なぞる（スクロール）と掴んで運ぶ（DnD）は、まだ前面へ出す道のまま。**
  AX には「その場所をなぞる」に当たる操作が無い。次はここ
- **Windows は 1 行も無い。**この実行ファイルの中身を差し替える形になる
  （`PostMessage(hwnd, WM_LBUTTONDOWN)` が同じ役をする）。**呼ぶ側の形は変えない。**
  人の言葉: 「だめなら最悪、win,macは別々のソースでお願いします」——
  **CLI の形（`press <pid> <x> <y>`）だけ揃えれば、中身は別々でよい**

**追記（同日・**なぞると運ぶで指が飛ぶのは、避けられなかった**）**

> スクロールと、DnDのときだけ一瞬前面にでるのかまうすぽいんたがとびます。
> なんかgit-qa上のマウス位置が実際のそのデスクトップアプリのいちに一瞬移動する感じですね。

**macOS では避けられない。**滑車の出来事に場所を書いて送ると（`CGEventSetLocation`）、
**OS がその場所へ指を動かす**（実測: 948,1521 → 1607,1630）。
指を動かさずに窓へ届ける口が無い。

**やったこと**

- 終わったら指を元へ返す（`CGWarpMouseCursorPosition`）
- **返した直後に繋ぎ直す**（`CGAssociateMouseAndMouseCursorPosition`）。
  返すだけだと **0.25 秒ほど実際のマウスの動きが伝わらず**、人には
  「斜めに引っ張られる」ように見えた
- **飛んでいる時間を 180 ms → 約 40 ms に詰めた。**届く最短を実測で探した
- 人が触った分を**1 つずつ順に送る**。同時に走らせると、前の操作が指を返す前に
  次が「元の位置」を覚えてしまい、なぞるほどずれていく

**残る見た目**

**押すときは飛ばない**（AX の要素を直接押すので、指も前面も動かさない）。
**なぞるときと運ぶときだけ、40 ms ほど指が飛ぶ。**

**Windows では消える見込みがある。**`WM_MOUSEWHEEL` は窓に直接送れるので、
指を動かす必要が無い。**ただし未実測。**

### C58: 他の案件の名前を、git-qa の記録に書かない（2026-09-07・**人が出した指示**）

**決めたこと**

git-qa は public。**追跡しているファイルに、別の案件・別の組織の名前を書かない。**
残すのは**踏んだ事実の方**で、どの製品で踏んだかではない。

```
書かない: 2026-09-07、○○くん（Electron）で丸一時間つまずいた所。
書く:     Electron / Chromium で作られたアプリは、聞かれるまで木を作らない。
```

**根拠（人の言葉）**

> 理由はまったく関係ない人がみたらこういう技術の製品で検証したは、意味がある。
> ○○くんといわれて、は？ってなります。

**これは秘密保持の話であると同時に、書き方の話。**読む人にとって
「Electron のアプリで踏んだ」は意味があり、製品名は意味を持たない。
**名前を外すほうが、文章としても正確になる**（特定の製品の話ではなく、その技術の話なので）。

**線の引き方**

| | |
|---|---|
| **`meta-taro` 配下** | 書いてよい。git-qa と同じ並びの持ち物で、読む人にも辿れる |
| **それ以外の組織** | **書かない。**製品名・リポジトリ名・Issue の URL のいずれも |

**やったこと（2026-09-07）**

追跡ファイル 18 個から外した。実装コメント・テスト・`.claude/` の記録の全部。
**履歴は書き換えない** —— 既に配った `develop` を壊すし、GitHub の cache や fork には残る。
**いま見に来た人が読むのは最新版**なので、そこを直せば実害はかなり減る。

**人にしかできないこと**

**`OSS_DENY_WORDS`（secret）へ語を足すのは人が行う。**
リポジトリに書くと、それ自体が公開になる（`oss-privacy-check.sh` の設計上の約束）。

**これを止めるべき条件**

- 事実だけにすると**何を指しているか分からなくなる**とき。
  そのときは名前ではなく、**技術の特徴で特定できるまで書く**（「Electron・Windows 前提・自前の題名帯」など）

### C59: 品質ゲートの出力を絞らない（2026-09-08・**自分の失敗から**）

**決めたこと**

`pnpm verify` の結果を `grep` で絞って見ない。**そのまま読む。**

**根拠**

2026-09-08、こう確かめていた。

```bash
pnpm verify 2>&1 | grep -E "Test Files|Tests |error TS" | tail -3
```

**テストの行だけが目に入り、lint の失敗が消えていた。**その状態で 2 回 commit した。
`tsc` の型検査も `error TS` で拾うつもりだったが、**lint は別の書式で出る。**

**絞る形にした理由は「出力が長いから」**で、それは見落としと引き換えにしてよいものではない。

**あわせて守ること（同日に外した）**

- **実装を先に書いて、落ちた既存テストを後から書き換えない。**
  `findInElements` に大きさを足したとき、それをやった。**先に落ちるテストへ直す。**
- **数字を足したらテストで留める。**`ARROW_ROOM`（矢印 1 つが要る余地）を
  26 → 38 へ動かしたとき、留めていなかった。**留めていない数字は、
  次に触った人が理由なく動かせる。**

**これを止めるべき条件**

- 無い。**出力が長いのは、絞る理由にならない。**

### C60: 建てる道具が要る検査は、配る直前に置く（2026-09-11・**CI を 1 度落として学んだ**）

**決めたこと**

**配布物が参照しているものを数える検査（§23）は、`pnpm verify` に入れない。**
**配布物を建てる直前に置く。**

**根拠**

2026-09-11、§23 の仕組みが無いことに気づいて `check:assets` を足し、
**普段のゲートに入れて push したら CI が落ちた。**

```
[git-qa] 参照されているのに存在しない:
  - resources/git-qa-input
```

Linux の runner には `xcrun swiftc` も `cargo` も無い。**建てられないのは当たり前**で、
**そこで落とすと、道具が無い環境での開発が全部止まる。**

**§23 の狙いは「配ったあとで初めて壊れる」を止めること。**だとすれば、
**配る直前に数えれば足りる。**普段のゲートで数える理由が無い。

**このとき、もう 1 つ見つかった**

CI のログで **`git-qa-ocr` だけが「無い」と言われなかった。**Git から降ってきていた。

```
$ git ls-files packages/desktop/src-tauri/resources/
packages/desktop/src-tauri/resources/git-qa-ocr   （Mach-O x86_64・37,808 bytes）
```

**建てて作るはずのバイナリが、public リポジトリに 2 回 commit されていた。**
`.gitignore` に足したのが**後**だったので、追跡が外れていなかった
（`git rm --cached` が要る。**`.gitignore` は既に追跡しているものを外さない**）。

**落ちた CI が、こちらを連れてきた。**落ちたまま放置していたら、気づいていない。

**採らなかった案**

- **検査を OS ごとにする** —— 普段のゲートに置いたままにするための工夫になる。
  **置き場所が違うことの方が本質**なので、そちらを直した

**これを止めるべき条件**

- 配布物を建てる手順が増えて、**「配る直前」がどこか分からなくなったとき。**
  そのときは「配る」を 1 本の手順にまとめてから、その中へ置く

**残っていた宿題 → 片付けた（2026-09-11）**

`tauri.conf.json` が **macOS 専用の道具を無条件で参照していた**（`git-qa-ocr` に加え、
その後 `git-qa-input` と `git-qa-record` が増えて 3 つになった）。
**このまま Windows 版を建てると、建つ前に `pnpm check:assets` で止まる。**

`tauri.macos.conf.json` へ分けた（Tauri が OS ごとの設定を自動で重ねる。**配列は置き換え**）。
`check:assets` も同じ数え方に揃えた。

```
macOS:   参照 9 個 / 無い 0 個
Windows: 参照 6 個 / 無い 0 個
```

**Windows で動かすのはウェブ検証と Android 検証**で、そもそもこの 3 つは要らない。
逃げではなく、**要らないものを要らないと書いた。**
**まだ 1 度も Windows 版を建てていない**ことは変わらない（`build-windows.yml` は
人が押して走らせる形のまま・実行 0 回）。

---

### C61: 相手が入れ替わったかは「止める」ではなく「残す」（2026-09-11・**外部レビュー #3 から**）

**決めたこと**

検証対象が走行中に入れ替わっていないかを、**指紋 1 本**で測る。
違っていても**実行は止めない。**証跡（`run.json` の `targetCheck`）と
報告（`pnpm check:run`）に**読める形で残す**だけにする。

指紋の作り方は**相手ごとに違う**ので、`TargetSession.fingerprint?()` は
**文字列を 1 本返すだけ**にした。同じかどうかを決めるのは core（`compareFingerprint`）。

**根拠**

外部レビュー（meta-taro/git-qa#3）の言い分がそのとおりだった。

> シートは同一性を持っているのに、検証対象は持っていません。
> 前半 5 件は旧ビルド、後半 5 件は新ビルド、という証跡を作ります。
> そして `run.json` を読んだ人には、それが 1 つのビルドに見えます。

実際に起きる形は地味で、**誰も悪意を持っていない。**同じ機械でその製品を
開発しているセッションが再ビルドする、`pnpm dev` がホットリロードする、
検証の途中で誰かが更新を当てる。

この道具の考え方は「止める」ではなく「**見る場所を絞る**」なので、
**変わったことが読めれば足りる。**

**「変わっていない」と「測れなかった」を混ぜない**（録画・画面と同じ）。
測る口を持たない相手に空文字を返させると、**無い保証があるように読める。**
だから `undefined` と `unmeasurable` を残した。

**採らなかった案**

- **レビューの提案 2（実行中はビルドをロックする）** —— 人の作業を止める側に倒れる。
  同じ機械で開発している人に「検証が終わるまで建てるな」と言うことになる。
  **こちらが困るからといって、人の手を止めない**
- **違っていたら実行を中断する** —— 途中で止めると、**人が既に置いた判定まで宙に浮く。**
  走り切って、読める形で残すほうが失うものが少ない

**これを止めるべき条件**

- **指紋が「変わっていない」と言ったのに、実際は変わっていた**事例が出たとき。
  実行ファイルを見る方式は、**中身だけ差し替わるホットリロードを拾えない**
  （Electron の renderer など）。そのときは、相手に版番号を聞く口へ寄せる

**実行状況**

`compareFingerprint`（core）・`fingerprint()`（デスクトップ）・`targetCheck`（schema と Run）・
報告への 1 行、まで入れた。テスト 17 本。**ウェブと Android はまだ口を持っていない**
（持たない相手は `unmeasurable` と出る）。

**追記（2026-09-11 夜）: 実物で走らせるまで、1 度も取れていなかった。**

最初は JXA で `NSRunningApplication` を呼んでいたが、**あれは Foundation ではなく AppKit。**
実物では毎回 `TypeError` で、呼び側が `undefined` に畳むので、
**証跡には「測る口を持っていない」と出ていた。**口はあったのに。

次に `ps -o comm=` へ替えたら、**あれは実行ファイルの場所とは限らなかった**
（名前を書き換えたプロセスでは `node (vitest 1)` が返る）。
いまは `lsof -d txt`（そのプロセスが実行している本体そのもの・`osascript` の 1/4 の速さ）。

**どちらも、文字列の形だけを見る検査では捕まらなかった。**
`exePathScript(1398)` が `"1398"` を含むことは、その API が在ることを何も言わない。
**実物で動かす検査を足した**（自分自身のプロセス番号を聞く。macOS でだけ走る）。

---

### C62: 鑑賞は 3 つ目のモードにする（2026-09-11・**人の指示**）

**決めたこと**

> auto を git-qa まんま操作するパターンを実装します。……人はぼーっとみながら
> AI のテストを鑑賞します。……途中で止められる配慮も必要です。

`mode` に **`'watched'`** を足した。既存の 2 つは借りない。

```
manual   … 人が全部操作
assisted … AI が操作し、人が押すまで次へ行かない
watched  … 人は見ているが、押さなくても進む   ← 足した
auto     … 誰も見ていない
```

録るのは **git-qa の窓**（人の判断）。止める口は **Esc と押しボタン**の 2 つ。

**根拠**

`assisted` は**人が押すまで止まる**、`auto` は**誰も見ていない**。
鑑賞はどちらでもない。どちらかの名前を借りると、**証跡を読んだ人が実際と違うものを
思い浮かべる。**`assisted` と書けば「人が 1 件ずつ見て通した」と読まれ、
`auto` と書けば「誰も居なかった」と読まれる。**どちらも嘘になる。**

`auto` を借りられない実際的な理由もあった。`executeRun` には
**「`auto` なら `askHuman` を渡せない」**という守りが入っている（誰も見ない実行の
はずなので）。鑑賞では人が押せるので、この守りと噛み合わない。
**守りを緩めるのではなく、名前を足した。**

録画を git-qa の窓にしたのは、**相手のアプリだけ録っても判定の根拠が写らない**から。
ライブ映像・いまどのケースを判定していたか・AI が何と言ったか・矢印がどこを
指していたかは、**全部 git-qa の窓の中にある。**

**採らなかった案**

- **`auto` を広げる** —— 上のとおり、読む人を誤らせる
- **鑑賞中は判定を受け取らない**（純粋に見るだけ）—— 見ていて気づいたことを
  その場で置けないのは、**この製品の値打ちを捨てている**
- **間を 0 にして一気に流す** —— それは `auto` であって鑑賞ではない

**これを止めるべき条件**

- **鑑賞で置かれた `VERIFIED` が、実際には見ていない判定だった**と分かったとき。
  間（既定 4 秒）が短すぎて、惰性で押されているということになる

**実行状況**

`watched` モード・`stop` 入力・鑑賞中の案内と止めるボタン・設定画面の選択・
git-qa の窓の録画（webm）まで入れた。テスト 38 本。
**CLI（`pnpm run:sheet:*`）には口を付けていない** —— 指示はアプリの話だった。

---

### C63: 試験 1 本を 1 つのフォルダにまとめ、続きから走らせられるようにする（2026-09-12・**人の指示**）

**決めたこと**

> 試験というプロジェクトファイルみたいなイメージでした。試験 tsv 画像動画が
> ひとつのワークスペースにまとまっているみたいな。そっちの方が試験単位の
> git 管理も便利です

```
<ワークスペース>/
  git-qa.json          ← ここが 1 本の試験だという印（pnpm ws:init で作る）
  <検証シート>.tsv
  runs/
    20260912-100000/
      run.json
      case-001/screen.webp · screen.webm
```

あわせて 3 つ変えた。

1. **証跡はワークスペースの `runs/` へ置く。**印が無ければ今までどおり
2. **シートの場所は、ワークスペースからの相対で書く**
3. **続きから走らせられるようにする**（同じ `runId` に足す）

**根拠**

「途中まで試験して、次の日再開できるか」と聞かれて数えたら、**できなかった。**
証跡は残るが続きから走らせる口が無く、翌日は新しい実行として 1 件目から走る。
**昨日人が見て置いた分を、もう一度置き直すことになる。**

まとめる形にしたのは人の判断だが、**やってみて 2 つ得があった。**

- **証跡から個人名が消えた。**それまで `sheet.path` は選んだままの絶対パスで、
  実物がこれ ——
  `"/Users/<個人名>/Documents/GitHub/git-qa/sheets/android-settings-ja.tsv"`。
  **git で管理するなら混ざる**（§25）。相対なら、誰の機械でも同じ形になる
- **渡す単位ができた。**フォルダごと渡せば、シートも画面も動画も揃っている

**採らなかった案**

- **シートの在る所を自動でワークスペースにする** —— 印が要らなくなって楽だが、
  **いまの使い方が黙って変わる。**`sheets/*.tsv` を使っている人の証跡が
  `sheets/runs/` へ移る。**印を人が置いたときだけにした**
- **続きを新しい実行にして、後で束ねる** —— 証跡が 2 本に割れる。
  読む人が突き合わせることになる
- **止めたケースを `BLOCKED` として書き続ける**（C62 でそう決めていた）——
  **走らせた末に判断保留になったケースと見分けが付かない。**
  続きから走らせるときに、どれをやり直すべきかが読めない。
  **走らせていないものは書かない**に変えた（`finishedAt` が無いことが「途中」の印）

**これを止めるべき条件**

- **1 つのシートを複数のワークスペースで走らせたくなったとき。**
  いまは「シートの家＝証跡の家」で固定している
- 続きから走らせたのに**シートが変わっていた**事例が続くとき。
  いまは `sha256` が違えば断る（昨日の判定は、いまの文面に対して置かれたものではない）

**実行状況**

`pnpm ws:init` ・ワークスペースの解決・続きから（core / 実行器 / 画面）まで入れた。
テスト 33 本。**実物で通した**（相手 warifu・見本シート 3 件）——
1 件走らせて止め、`finishedAt` が付かないことと、翌日の続きで
1〜3 件が 1 本の証跡に揃うことを確かめた。`run.json` に `/Users/` は 0 件。

### C64: 判定できるのは「画面に出る文字」だけ。言い換えを勧めない（2026-09-16・**外部レビュー #27 から**）

**決めたこと**

期待結果を機械で判定するのは、**鉤括弧が 1 組**で、かつ**その後ろが「画面に出る文字を
指す言い方」**（「〜と表示される」「〜が出ている」）のときだけ。それ以外は**判断保留。**

**根拠**

述語を見ずに落としていたので、**判定として成立しない行が「機械で判定できた行」として
結果に並んでいた。**

```text
contains("定休日")       <= 「定休日」の日は押せない          ← 押せることを確かめずに合格
contains("選択できない")  <= 「選択できない」状態になっている   ← 実装が正しくても必ず落ちる
```

**いちばん効いた指摘は、但し書きのほう。**

> **但し書きに従うほど、判定が間違った方向に確定します。**

「画面で探す文字列が無い」と読んだ人は**鉤括弧を足す。**実際それで通り、
**hold（正直に判定を放棄した状態）から、言っていることの逆を判定する状態へ移る。**
**道具が、人を間違いへ案内していた。**

**採らなかった案とその理由**

- **言い回しを増やして、もっと落とす** —— 曖昧なものを当てにいくほど、
  **別のことを判定する**確率が上がる。**判断保留は失敗ではない**
- **「押せる」を判定できるようにする** —— いまの口（画面の文字）では確かめられない。
  **確かめていないことを「確かめた」と書かない**

**これを止めるべき条件**

判断保留が多すぎて**人が全部見る羽目になった**とき。ただし**減らす方向は
「言い換えを勧める」ではなく「確かめる口を増やす」**（例: 押せるかどうかを相手に聞く）。

---

### C65: 無人で走らせてよい。ただし「無人だった」と証跡に残す（2026-09-16・**外部レビュー #21 から**）

**決めたこと**

`--no-ui` で**誰も見ていない実行**ができる。出るのは `AUTO_PASS` 止まりで、
**`VERIFIED` は型として書けない**（C17）。証跡には `mode: "auto"` が残る。

終了コードで**朝に開くべきものが在るか**が分かる（0 = 全部通った / 1 = 落ちた行 /
2 = 判断保留。**空の実行も 2** —— 「1 件も走っていない」を「通った」と読ませない）。

**根拠**

> 夜間にひととおり流して、**落ちた行と証跡だけを朝に人が見る**

`RunMode` に `'auto'` は在ったのに、**それを作るコードがどこにも無かった** ——
**語彙としては在るのに、外から到達できない**状態だった。

要望した側が**先に線を引いてくれた**のが大きい。

> **AI に `VERIFIED` を置かせてほしい、という話ではありません。**

**これを止めるべき条件**

**`mode: "auto"` が読む人に届かなくなったら止める。**朝に証跡を開く人が
「どの行を人が見て、どの行を機械が流しただけか」を取り違えるなら、無人実行は
**証跡としての用途をそのまま消す。**

---

### C66: 起こしたものは、自分で片付ける。他人のものは片付けない（2026-09-15〜17・**自分の失敗から**）

**決めたこと**

1. **合図（`SIGINT`）を受けたら、その場で落とす**（非同期を挟まない）
2. **閉じるときに、落ちたことを確かめる**
3. **次に起こすとき、置き去りがあれば片付ける** —— **親がもう居ないものだけ**
4. **繋いだだけのブラウザは閉じない**（#30。Playwright が使っている最中かもしれない）

**根拠**

切り分けのために起動と強制終了を繰り返し、**検証用の Chrome を 30 個溜めた。**
人からはこう見えた —— **「スパムで自分を殺す癌みたいなやつだ」。**

**1 が要る理由は実測で分かった。**`close()` を辿る道を入れたのにブラウザは残った ——
**同じプロセスの vite も合図で終わる**ので、後始末が最後まで走らない。
**非同期を 1 つ挟むだけで、そこで終わる。**

**緩めないところ**

- **人が普段使っているブラウザには触らない**（仮置き場の印つきプロファイルだけ）
- **いま走っている実行のブラウザにも触らない**（親が居ないものだけを置き去りと見なす）

**ここを雑にすると、人の開いているタブを消す** —— いちばんやってはいけない壊れ方。

---

### C67: 操作は Playwright、判定は git-qa（2026-09-17・**人の指示**）

**決めたこと**

**Playwright を再実装しない**（PRD の非目標のまま）。代わりに、

- **既に起きているブラウザへ繋ぐ**（`GIT_QA_CDP`）。**起こさない・閉じない**
- **MCP でウェブ・デスクトップも AI が触れる**（`element_tap` で名前で押す）
- **文書で Playwright を強く勧める**

**根拠**

> playwright も git-qa 導入者には**強く推奨**となるような説明や MCP 構築でお願いします

**検証シートの語彙は、わざと狭くしてある**（曖昧なものを当てにいくと、判定が別のことを
見る・C64）。**そこを埋めるのが Playwright。**役割が違うので競合しない。

| | やること |
|---|---|
| Playwright | **決まった操作を確実に繰り返す**（ログイン・データ用意・複雑な手順・CI） |
| git-qa | **人が見て判定を置き、証跡に残す** |

**このとき、文書が「Web | Adapter（Playwright）」のままだった**（実装は CDP 直）。
**期待の出どころがそこだった** —— 文書が古いと、**使う人は実装ではなく文書を信じる。**

**これを止めるべき条件**

**MCP に判定を置く道具を載せたくなったら、そこで止める。**載せた瞬間、
AI が「人が見た」と書けてしまう（C1 / C17）。


---

### C68: 開いている選択肢は、システム全体の口から当てる（2026-09-18・#32 の実測）

**決めたこと**

`git-qa-input press` は、アプリの口で当たらなかったとき
**システム全体の口（`AXUIElementCreateSystemWide`）からも当てる。**
ただし**当たった相手のプロセス番号を確かめ、相手のものでなければ押さない。**

外部レビュー（meta-taro/git-qa#32）が勧めた **`CGEventPostToPid` は採らない。**

**根拠（2026-09-18 実測・git-qa 自身の窓＝Tauri／WebKit）**

相手は Terminal を前面にしたまま、ブラウザ選択の `AXPopUpButton` を押した。

| 送り方 | 結果 | 前面 |
|---|---|---|
| `CGEventPostToPid` | **メニューが開かない** | Terminal（動かず） |
| `AXPress` | `AXMenu` と選択肢 8 個が木に出た | Terminal（**動かず**） |
| `AXPress`（選択肢 Firefox） | `AXPopUpButton` が `Firefox` に変わった | Terminal（**動かず**） |

**`CGEventPostToPid` は Chromium / Electron に続き、WebKit でも受け取られない。**
描画器を問わず届かないので、道として使えない。

**`AXPress` は前面を奪わずに、開く → 選ぶまで通る。**
**通らなかったのは「押し方」ではなく「当て方」だった** —— `<select>` の選択肢は
**アプリの窓の外**に出るので、アプリの要素を起点にした当て方では見つからず、
**座標を押す道（前面へ出る道）へ落ちていた。**出した拍子に、その選択肢が畳まれる。

**採らなかった案とその理由**

- `CGEventPostToPid` … 上の実測。**呼び出しは成功を返すのに、何も起きない**
- 前面へ出したあと戻す … beta.8 で試した。**戻す操作そのものが選択肢を畳む**

**これを止めるべき条件**

システム全体の口で**他人の窓を押してしまう**ことが 1 度でも起きたら、そこで止める。
いまはプロセス番号で守っているが、**守りが破れたら座標を押す道へ戻す。**

**自動試験は無い。**アクセシビリティの当て方は、生きたアプリが要る（実測で確かめた）。

---

### C69: ウェブの中身は、押せる親まで登って押す（2026-09-18・#30 の実測）

**決めたこと**

`git-qa-input press` は、当たった相手が押せないとき**押せる親まで登る**（5 段まで・
相手のアプリの中だけ）。切り分けのために **`at` 口**（その場所に何が在るかを言う）を足した。

**Chrome をデスクトップ検証で見るときは `--force-renderer-accessibility` が要る。**
これは**使う人が起動時に付けるもの**で、git-qa からは付けられない（起動時にしか効かない）。

**根拠（2026-09-18 実測・使い捨てプロファイルの Chrome 1 個。測定後に落とし、0 を確認）**

| 相手 | `AXPress` | 結果 |
|---|---|---|
| Chrome のネイティブ部分（ブックマーク・閉じる） | `ok` | **効く**（泡が開いた） |
| ウェブの中身（旗なし） | `ok` | **何も起きない** |
| ウェブの中身（`--force-renderer-accessibility`） | `ok` | **「押された」に変わった** |

**当たる深さが、そのときどきで違った。**同じ場所（1241, 649）で、
`AXButton 押す`（100x44）が返るときと、**その中の `AXStaticText 押す`（32x17）**が返るときがある。
後者には `AXPress` が無いので「その部品は押せない」で落ちていた。
**押せたり押せなかったりする**のが、いちばん質の悪い壊れ方なので、**登って吸収する。**

**端から端まで通ることを確かめた**（`runs/20260918-174638`）。

    AUTO_PASS 1 — 「押す」をクリックする → 「押された」と表示される
    実行前の前面: Terminal / 実行後の前面: Terminal   ← 奪っていない

**Playwright が操作し、git-qa が見て人が判定を置く**（C67）は、**デスクトップ検証として成立する。**

**採らなかった案とその理由**

- **git-qa 側で旗を付けて Chrome を起こす** … #30 の話は「**既に起きているもの**へ繋ぐ」。
  起こす側に回ると、**他人のブラウザを片付ける話に戻る**（C66 / #20 の逆側）
- **登る段数を無制限にする** … 窓や application まで登ると、
  **押した場所と関係の無いものを押す。**5 段で止める

**これを止めるべき条件**

**押した覚えの無いものが押されたら、そこで止める。**登りすぎている。

---

### C68 の裏取り: dbboard の実物で通った（2026-09-18 夕）

**報告された相手そのもの**（dbboard・Tauri）で、`beta.12` の道具を使って 4 件流した。

    無人で走らせた（誰も見ていない）: 4 件 — AUTO_PASS 4 / FAIL 0 / BLOCKED 0 / SKIP 0
    実行前の前面: Terminal / 実行後の前面: Terminal

**開く → 選ぶ → 開き直す → 戻す**まで、**一度も前面を奪わずに**通った。
開いた `AXMenu` の選択肢 11 個は木に出ており、**シートの文字のまま**選べる。

**見本のアプリではなく、報告された実物で測った。**
`AXPress` が前面を奪わないことも、選択が効くことも、これで両方確かめた。

**証跡には接続先が写る。**`runs/` は `.gitignore` に入っている（公開リポジトリ・§25）。
**Issue にも貼らない。**測定後に dbboard は終了し、言語も元へ戻した。

---

### C70: 配った版は、自分の名乗りを持つ（2026-09-18）

**決めたこと**

**インストーラの版番号は触らない**（`tauri.conf.json` / `Cargo.toml` は `0.2.0` のまま）。
**建てるときに `GIT_QA_RELEASE` でタグを渡し、名乗りだけを本物にする。**

- Rust: `option_env!("GIT_QA_RELEASE")`。無ければ `0.2.0-dev`（**配ったものと混ぜない**）
- 画面: 準備の画面に「この git-qa は v0.2.0-beta.12 です」
- 証跡: `runner: { name, version }`（スキーマが正本・C18）
- CI: タグを打ったときだけ本物。手で走らせたときは `dev`

**根拠**

**`beta.3` も `beta.12` も、名乗りは `0.2.0` のままだった。**
About ダイアログに版を出す所は在ったのに、**そこに出ていたのは常に同じ数字**だった。

**「どの版を使っていますか」が聞けない。**試験導入先で `beta.8` の修正が効いていない
場面があり、**こちらも相手も、何を触っているのか確かめられなかった。**

**証跡にも道具の版が無かった。**「誰が・いつ・何を見て判定したか」は残るのに、
**それを出した道具だけが残らない。**後から「その判定は、直る前の版で出たものだ」と
言えない。

**採らなかった案とその理由**

- **`version` そのものをタグにする** … **MSI は `major.minor.patch` しか受け取らない。**
  `0.2.0-beta.12` を入れると **Windows の配布が壊れる**（#36 の直前まで Windows は
  起動すらしていなかった。同じ所をもう一度壊さない）
- **画面から版を消す** … 報告に版が書けないままになる

**これを止めるべき条件**

**タグを打って建てたのに `dev` と名乗ったら、そこで止める。**
焼き込みが効いていない（`build.rs` の `rerun-if-env-changed` が要る）。

**実行状況**

実測で確かめた（`cargo test`）。

    渡さない          → 0.2.0-dev
    v0.2.0-beta.13    → v0.2.0-beta.13
    もう一度渡さない   → 0.2.0-dev      ← 建て直しが効いている

---

### C71: 勝手に入れ替えない。出ていることを言うだけ（2026-09-18）

**決めたこと**

**自動更新（本物の updater）は入れない。**
**新しい版が出ていることを準備の画面に出し、押したら配布ページを開く。**入れるのは人。

**根拠**

**2 日で 10 本出した。**受け取る側は、**新しいのが出たことを知る手段を持っていない。**
配布ページを見に行くしかなく、**古い版のまま不具合を踏み続ける**（実際に起きた）。

一方で、**人が見ないまま外へ出す／中へ入れる経路を作らない**のがこの repo の土台
（`product-baseline.md` §6）。**入れ替えは、人が見て決める工程**に残す。

**依存は足していない。**Node の `fetch` で足りる（C54 と同じ考え方）。

**採らなかった案とその理由**

- **`tauri-plugin-updater`** … **署名鍵が要る**（秘密情報の生成と投入は人の作業・§14）。
  さらに **Windows は未署名**なので、入れ替えのたびに SmartScreen が出る。
  **黙って入れ替わる経路**を先に作ると、後から止めにくい
- **`releases/latest` を見る** … **prerelease は返らない**（実測で 404）。
  beta のあいだは全部 prerelease なので、**誰にも通知が届かない。**
  一覧を取って、**自分より新しいものを自分で選ぶ**
- **リンク（`<a href>`）で出す** … アプリの窓は CSP で外へ出られない。
  **押しても何も起きないリンク**は、何も出さないより悪い。OS のブラウザへ渡す
  （**URL は画面から受け取らない** —— 任意の場所を開く口になる・§21）

**これを止めるべき条件**

- **最新の版なのにお知らせが出たら**、そこで止める（版くらべが壊れている）
- **繋がらない機械で画面が止まったら**、そこで止める（聞きに行くのを外す）

**実行状況**

本物の GitHub で確かめた。

    beta.9  → {"version":"v0.2.0-beta.12","url":"https://meta-taro.github.io/git-qa/"}
    beta.12 → undefined     ← 最新の人は急かされない
    0.2.0-dev → undefined   ← 手元で建てた人は、そもそも聞きに行かない

---

### C72: どのプロファイルかは選べる。ただし黙って私物を見ない（2026-09-18・#35）

**決めたこと**

`GIT_QA_PROFILE_DIRECTORY`（`--profile-directory`）を**足す。**
**普段使いの置き場所を指したら、大きな声で言う**（止めはしない）。
**証跡に `target.profile` を残す**（`fresh` / `provided` / `personal`）。

**根拠 —— 扉は、もう開いていた**

外部レビュー（#35）は「この提案はそちらの方針と正面から衝突する」と**自分で書いてきた。**
だが、衝突は**半分もう起きていた。**

    GIT_QA_PROFILE="%LOCALAPPDATA%\Google\Chrome\User Data"   ← 今でも渡せる
                                                              → Default が使われる

**`Default` は、たいてい**その人の**いちばん私物のプロファイル**。
**いま最も危ない指し方が、いま何の見張りも無しに通っていた。**

`--profile-directory` は**危険の種類を増やさない。**増やすのは**精度**。
**足りていなかったのは口ではなく見張り**だった。

**採らなかった案とその理由**

- **拒否する** … **報告者の使い方を塞ぐ。**専用に用意されたプロファイルは
  `User Data` の**中**に居るので、標準の場所を一律で止めると、
  **いちばんまともな運用が通らなくなる**
- **口を開けない**（README に「指せない（意図的）」と書くだけ） …
  588 MB の複写を手順として案内するのは重い。
  **専用アカウントを用意している運用が既に在る**のに、「ログインし直せ」で切れない

**これを止めるべき条件**

**普段使いのプロファイルが検証で触られた事故が 1 度でも起きたら、拒否へ切り替える。**
**そのために、証跡へ残すほうを先に入れてある**（後から「あの実行は私物だった」と言える）。

**既に効いている歯止め**

- **普段のブラウザが起きている間、置き場所はロックされる**（人の作業中には起動できない）
- **預かったプロファイルは消さない**（`shouldRemoveProfile`）

**実行状況**

この機械の実在するパスで確かめた（**中身には触っていない**）。

    ~/Library/Application Support/Google/Chrome → personal
      「普段使いのブラウザの置き場所にあるプロファイルを使った（その人の私物を見ている）」
    ~/qa-profile                                 → provided
    渡さない                                      → fresh

---

### C73: 各々の OS で開発するための環境（2026-09-19・人の指示）

**決めたこと**

1. **`pnpm doctor`** —— その機械で何ができるかを**測って**出す。柱ごと（見る・読む・押す・残す）
2. **開発版は転がる `dev` 1 本**。`develop` に入るたび CI が建てる。**Windows だけ**
3. **`docs/dev-check.md`** —— 誰が何を流して、何を貼るか

**根拠**

> 開発版としてプッシュして、win 側の開発動作確認を渡して各々の OS で開発していきましょう

**「自分の機械では動いた」は、そのままでは他の人に渡せない。**
何が在って何が無いかを**同じ形で**言えないと、「動かない」の原因が
**機械の差なのか、こちらの不具合なのか**が分からない。

実際、**Windows 版は配ってから 368 ms で死んでいたのに、誰も気づけなかった**（beta.9 で判明）。
**測る口が無いと、壊れていることが「仕様に見える」。**

**採らなかった案とその理由**

- **開発版で macOS も建てる** … **署名と公証が無い `.dmg` は「壊れているため開けません」**になる。
  **渡せないものを置くことになる。**mac 側は手元から動かす（`pnpm app`）のが普通の道。
  **mac の開発版が要るようになったら、署名の段を足す**（release.yml と同じもの）
- **`doctor` を品質ゲートにする**（欠けていたら 1 で返す）… **CI で落とすためのものにすると、
  人が測るのをやめる。**これは診断であって、ゲートではない
- **`develop` の全 push で建てる** … 文書だけの変更で走者を起こすのは高い。`packages/**` などに限る

**これを止めるべき条件**

- **`dev` の置き直しが `v*` に触ったら、即座に止める**（消すのは `dev` だけ）
- **健全な機械が「欠けています」と出たら止める** —— 毎回そう出ると、**人は読むのをやめる**
  （実際 1 度踏んだ。macOS で `git-qa-win` を欠けている扱いにしていた → `skip` を足した）

**実行状況**

この機械で測って、2 つ直した。

- `xctrace list devices` は **`== Devices Offline ==`** を「つながっている」と数えた（この Mac 自身も並ぶ）
  → `devicectl list devices` の **State** を読む形にした
- **列の幅で切ると日本語の端末名で崩れる** → 状態の語を探して、その後ろを機種とする

---

### C74: iOS は要る（**一度誤って見送りに書いた。取り消した**・2026-09-19）

**決めたこと**

**iOS は要るものとして残す。**ロードマップ Phase 6 と `.claude/issues/001` は**生きている。**

**なぜこの項が在るか**

**私がこの項を最初「見送り」として書いた。**人の次の言葉を、git-qa の話として受けたため。

> iPhone？デスクトップアプリなのでスマホは意識してませんでした。
> ただ PC を踏み台にしたリモート利用できるアプリは将来的にありですね。
> アプリ単体の割符は理想ですが、OSS なのでそこまで考えたら有料版とかになりますね

**これは割符（別のプロダクト）の話だった。**人からすぐに訂正が入った。

> あ、ごめん gitqa は検証に必要です。割符と勘違いしました

**消さずに残す**（§24）。**教訓はこちら側にある** ——
**プロダクトが複数あるとき、「デスクトップアプリ」がどれを指すかは文からは決まらない。**
**決定として書く前に、どのプロダクトの話かを確かめる。**
決定は**実装の前提になる**ので、取り違えたまま残すと、後から読む人がそれを根拠にする。

**リモート利用の話も、割符の話として扱う。**git-qa の方向として記録しない。

**iOS で要るもの（実装前の整理・未実測）**

| | Android | iPhone（macOS） |
|---|---|---|
| **見る** | `adb` / `scrcpy` | **USB で映せる見込み**（macOS は iPhone を撮影機器として見る）。**未実測** |
| **読む** | `uiautomator dump` | AX 木は WebDriverAgent 頼み。**ただし OCR の段は既に持っている**（C55） |
| **押す** | `adb shell input tap` | **WebDriverAgent を端末に入れるしかない**（署名＝人の作業・§14） |

**「見る」と「読む」は署名なしで届く見込み。**その形でも
**人が触って git-qa が見て人が判定を置く**（#30 / #35 と同じ分担）は成立する。

**これを止めるべき条件**

**「恐らく動く」を実測の欄に書いたら、そこで止める**（C56）。
iOS はまだ 1 行も測っていない。


---

### C75: iPhone / iPad は USB で映す。押す口は持たない（2026-09-19・叩き）

**決めたこと**

**`@git-qa/adapter-ios` を足した。**持つのは**見る・読む・録る**だけ。**押す口は持たない。**

| | やり方 |
|---|---|
| **見る** | macOS が **USB の iPhone / iPad を撮影機器として見せる**道（AVFoundation）。**端末に何も入れない** |
| **読む** | 絵から文字を読む（`git-qa-ocr`・C55 の段 2）。**AX 木は取れない** |
| **録る** | **流れてくる絵から作る。**Android と違い、OS の録画を借りない |
| **押す** | **持たない。**WebDriverAgent が要り、**それを入れるには署名が要る**（§14） |

`target.kind` に **`ios`** を足した（スキーマが正本・C18）。**Android に混ぜない** ——
触れ方が違うので、**証跡を読む人が「Android と同じように操作した」と読んではいけない。**

**根拠（人の指示）**

> Android を接続して検証録画できるように、iPhone,iPad の検証録画も必要です
> これの優れた点はスマホのアプリだけでなく**ブラウザを経由したウェブテストもできること**です

**録画が「要る」側に入ったのが大きい。**iOS では**映す道と録る道が同じ**
（流れてくる絵をそのまま残す）ので、**見るを作れば録るも付いてくる。**
Android は OS の録画を借りているので、そこが別物だった。

**ブラウザ経由のウェブ検証**

- **Android の Chrome** は `adb forward` で引き込めば、**既に在る `GIT_QA_CDP`**（#30）で繋がる。
  **道具も依存も足していない。未実測**
- **iPhone の Safari** は画面ごと見る（押す口が無いので人が触る）。
  **WebKit Inspector には繋いでいない** —— 別の道具が要るので、
  **まず画面を見る道が通るかを測ってから決める**

**採らなかった案とその理由**

- **WebDriverAgent を先に入れる** … **署名鍵の投入は人の作業**（§14）。
  **こちらだけで完結しない道を、最初の一歩にしない**
- **`.external` を使って macOS 14 以上を要求する** … 共通の建て方は macOS 13 向け。
  **古い名前（`.externalUnknown`）も持たせた** —— 指しているものは同じ
- **`Target['kind']` に `android` として混ぜる** … 触れ方が違う。証跡で取り違える

**これを止めるべき条件**

**「恐らく動く」を実測の欄に書いたら、そこで止める**（C56）。
**実機で 1 行も流していない。**`README` と `docs/dev-check.md` に、そう書いてある。

**実行状況**

- 道具は **universal（arm64 / x86_64）で建つ**。配布物にも同梱した（`check:assets` が 10 個を数えた）
- 端末が無いとき：`devices` は 0 行・終了 0、`shoot` は理由を出して 1、
  `pnpm run:sheet:ios` は**理由を出して止まり、何も残さない**
- **建てて 2 つ踏んだ** —— `.external` は macOS 14 以降、`CIImage` は `CoreImage` の取り込みが要る

---

### C75 の詰め（2026-09-19）— **数え直して、自分の入れた嘘を 2 つ直した**

**人の問い。**

> 妄想範囲では作りきりましたか？

**数えたら 4 つ残っていた。**うち 2 つは**こちらが入れた矛盾**だった。

| | 何 | 直した？ |
|---|---|---|
| ① | `capabilities.recording: true` と名乗っていた。**ウェブは同じ仕組みで `false`** —— この旗は「**アダプタ自身が**録れるか」の意味 | ✅ |
| ② | `fingerprint` に**端末の識別子をそのまま**入れていた。**同じファイルで「識別子は残さない」と書いた直後に** | ✅ 短く潰した |
| ③ | **MCP が `ios` を知らなかった** —— AI が画面を 1 文字も読めない | ✅ |
| ④ | **画面（`pnpm app`）から iOS を始められない**。CLI だけ | ❌ **実測待ち** |

**④ を今やらない理由。**`.claude/issues/001` が言うとおり、**問いは「実用的な遅延で見られるか」。**
**成立しなかったら iOS は AUTO 専用になり、製品が別物になる。**
**測る前に画面を作ると、境界を引き直すことになる。**

**確かめたこと（押せないときの振る舞い）**

`act` が投げると `actAll` が拾い、**`BLOCKED`＋理由**になる（`case-runner.ts`）。
**その上に人の判定を置ける**（`resolveCaseResult` は `humanResult` を先に見る）。

    「「保存」をクリックする」で止まった: iPhone / iPad を操作する口はまだ無い（tap）。…

**これは正しい形。**「操作できない」を `FAIL` にしない（C20）。
**人が端末を触って、映像で確かめて、1 打鍵で置く。**

---

### C76: 押す前に、押せる名前を並べる（2026-09-19・MCP 拡充）

**決めたこと**

MCP に **`element_list`** を足した。**契約は 1 つ。**

> **ここに並んだ文字は、そのまま `element_tap` に渡せる。**

**根拠**

`element_tap`（名前で押す・C67 の続き）は在ったのに、
**AI には「どんな名前が在るか」を知る口が無かった。**
できるのは画面の文字を読んで**名前を推し量る**ことで、**外れたときにしか気づけない。**

**座標を当てにいかないこと**は前から書いてあったが、
**名前を当てにいくことは止められていなかった。**同じ穴だった。

**集め方を、探し方と揃えてある**（**別の集め方を作らない**）。

| 相手 | 出どころ |
|---|---|
| ウェブ | `listElementsScript`（`findElementScript` と同じ集め方・見えているものだけ・上限 120） |
| Android | `listElementNames`（`findElementCenter` が当てにいく属性だけ・位置が取れるものだけ） |
| デスクトップ | `observe()` の `elements`（**元から持っていた。出していなかっただけ**） |
| iPhone / iPad | **理由を言って断る**（押す口が無い） |

**揃えないと「並んだのに押せない名前」が出る。**それは、無いより悪い。

**空のときに黙らない**（C20）。「画面に何も無い」と「この相手では読めない」を混ぜない。

**実測（2026-09-19・使い捨ての Chrome 1 個。測定後 0 を確認）**

    element_list → ローカルで動いているページ / まだ押されていない / 押す
    element_tap（並んだ最後のものをそのまま渡す） → 押した: "押す"
    device_screen_text → 押された

**契約が通ることを、端から端まで確かめた。**

---

### C77: 文書の古さを数えた（2026-09-19）

**人の指示。**

> MCP 拡充や README.md が古くなってないかチェックしてください

**8 箇所が実装と食い違っていた。**いちばん悪いのは `docs/alpha-guide.md` の 1 行。

    ログインが要る画面は、シートの 1 件目からログイン手順を書いてください

**これは #26 で「ID とパスワードをシートに書くことになるので禁じられている」と
言われて直した所。**実装は `GIT_QA_PROFILE` に変わっていたのに、
**文書だけが、禁じられたやり方を案内し続けていた。**

| 文書 | 何が古かった |
|---|---|
| `docs/agent-brief.md`（**MCP が全 AI へ返す**） | iOS「1 行も無い」／ウェブの録画が「無い」／「毎回まっさら」／`screen.mp4`（実際は `screen.webm`）／録画が「Android のみ」 |
| `docs/alpha-guide.md` | 上のログイン案内／iOS「ありません」／ウェブの録画 |
| `README.md`（英語） | iPhone / iPad が無い／MCP の対象／録画が「watch mode」だけ／`doctor` と版の名乗りが無い |
| `README.ja.md` | MCP の道具に `element_list` が無い |

**`site/index.html` は触っていない。**iOS は**実機で未実測**なので、
配布ページに書くと**はったりになる**（C56）。

**教訓は場所の重み。**`docs/agent-brief.md` は **MCP の `about` がそのまま返す** ——
**古いと、全部のエージェントに古い前提が渡る。**ここは README より重い。

---

### C78: clone の古さは、書くのではなく測る（2026-09-21）

**決めたこと**

`pnpm doctor` の**いちばん上**で、**この clone が上流よりどれだけ遅れているか**を出す。

**根拠**

Windows の開発機の clone が **`beta.3` で止まっていた**（11 版・約 5 日・別セッションの実測）。
**誰も気づかなかった。**その機械には **`beta.9` の Windows 起動修正**すら入っておらず、
**配った版が 368ms で死ぬまま**だった。

**そこから返ってくる値は、こちらでは使えない** —— 何を測っても、古いものの話になる。

`docs/dev-check.md` に「まず `git pull`」と書いた。**それだけでは足りない** ——
管理リポの記録がそう言っている（`[[feedback-workflow-mechanize-over-document]]`）。

> **ドキュメントに step を足す対策は「実行者が飛ばせる」ため無効**

**いちばん上に置く。**ここが古いと、下の値は全部「古いものの話」になるため。

**取ってきていない clone は、遅れを 0 と答える。**だから**いつ取ってきたか**も見る ——
**「追いついている」と「確かめていない」を混ぜない。**

**ここでは取ってきに行かない。**`doctor` は測る道具で、人の repo を動かす道具ではない
（勝手に `fetch` すると、**測っただけのつもりが状態を変える**）。

**踏んだ穴（2026-09-21）**

最初 `git log -1 --format=%ct FETCH_HEAD` で「いつ取ってきたか」を取っていた。**別物だった** ——
返るのは**取ってきた commit が作られた時刻**で、相手が 2 日前に commit していれば、
**さっき取ってきても「2 日前」**になる。実際、**自分の clone を「2 日、取ってきていない」と誤って言った。**

**`.git/FETCH_HEAD` の更新時刻**に直した。**1 度も取ってきていない clone では file が無い**ので、
そのときは日数では言わない（**遅れの数のほうが確か**）。
