# 019 — 試験運用（最初に使う 2 つ）

- Phase: 1
- 前提: 015（ウェブ）/ 016（デスクトップ）が動くこと
- 優先度: **最高**

## 人が決めたこと（2026-09-07・**言葉のまま**）

> 一旦試験運用は連絡くんという開発中デスクトップアプリと、ベガスギフトという開発中ウェブサイトで
> 考えています。この2つは私が担当していてｗ，ここで試験運用で、ブラッシュアップされたら、
> 別のウェブサイト開発中案件に試験運用として使ってもらいます。

## なぜこの選び方がよいか

**自分が担当している 2 つで先に踏む。**詰まったときに、原因を追える人がその場にいる。
他人の案件で先に踏むと、「道具が悪いのか、案件が特殊なのか」が切り分けられない。

**そして 2 つで種類が違う。**片方がデスクトップ、片方がウェブ。
**両方の道が実務で当たる。**

| | 種類 | 使う道 |
|---|---|---|
| **連絡くん** | デスクトップアプリ（開発中） | `adapter-desktop`（AX → OCR） |
| **ベガスギフト** | ウェブサイト（開発中） | `adapter-web`（Chrome / Firefox / Safari） |

そのあと**別のウェブ案件へ渡す** —— **他人が使って初めて、案内が足りているかが分かる。**

## 着手前に塞いだこと（2026-09-07）

**配布物からデスクトップ検証が選べなかった。**`pnpm run:sheet:desktop` はターミナルの話で、
**連絡くんを試すには足りていなかった。**

- 画面の「1. 見る相手を選ぶ」に**アプリ名の欄**を足した
- 実行器は形で見分ける（`http(s)://` ならウェブ、`app:` ならデスクトップ、それ以外は端末）
- **絵から文字を読む道具（段 2）を配布物へ同梱した。**
  それまでは環境変数で外から渡す形で、**配った先では段 2 が使えなかった**

## 見たいこと（試験運用で）

- **どこで止まるか。**止まった文が、そのまま人に届いているか
- 手順の語彙が足りているか（**足りない書き方が出たら、そこで足す**・C40）
- 同じ文字が複数あるとき（一覧の「削除」が 10 個など）に、どう書きたくなるか
- ログインが要る画面で、毎回まっさらなプロファイルが邪魔にならないか
- デスクトップで、段 1（AX）が空のときに段 2（OCR）で足りるか

## 完了条件

- **連絡くんで、人が 1 本通して `run.json` を出している**
- **ベガスギフトで、人が 1 本通して `run.json` を出している**
- 出た指摘が Issue になっている（**直っていなくてもよい。落ちていないことが条件**）

## 渡し方（2026-09-07・**dmg をやめた**）

**当初は dmg を渡すつもりだった。**署名・`xattr -dr com.apple.quarantine`・Release の作成・
`0.0.0` という版番号 —— **どれも試験運用の本題ではない。**

人の指摘で前提が変わった。

> あの、向こうのデバイスにも割符のクローンリポジトリがあるんですよ

**git-qa は public。**なら渡す物は要らない。**クローンして動かしてもらえばよい。**

```bash
git clone https://github.com/meta-taro/git-qa.git
cd git-qa && pnpm install
GIT_QA_OPERATOR=<名前> pnpm run:sheet:web <シート.tsv>
```

これで消えたもの: 署名 / `xattr` の 1 行 / GitHub Release / 版番号の説明。
**そして直したものが `git pull` で届く。**アルファ版の間はこちらのほうが速い。

割符は**渡す道ではない**（README を読んだ: 2 人の映像と文字、会議キー、同じ網の中、
**ファイルは送れない**）。**初回に立ち会う道**として使う。**それ自体が割符の試験運用になる。**

## 出したもの

- **ベガスギフト**: https://github.com/dokokade/vegas-gift/issues/30 （2026-09-07）
- **連絡くん**: まだ出していない

Issue #30 に書いたこと（**言い過ぎていないか、ここで自分を縛る**）:

- 確かめたのは **Apple Silicon の Mac だけ**。**Windows では 1 度も走らせていない**と明記した。
  ベガスギフト側には powershell の script がある（`monitor:install`）ので、
  **相手が Windows である可能性が高い。**そこで「動きます」と書かなかった
- ログインは引き継がない。**Supabase の確認メールを踏む導線はいまは通せない**と書いた
- Stripe の決済導線は試していないと書いた
- 期待結果は **8 言語のどれで出るかを先に見てから書く**よう頼んだ（next-intl）
- **速さの数字は 1 度も測っていない**と書いた（Issue 008）

## 連動くん = `dokokade/browser-sync-agent`（2026-09-07 に実機で確かめた）

Electron + TypeScript + Vite。**Windows 前提で書かれている**（`Win+←`・エクスプローラー・
タスクバー・`docs/brand/logo-prompt.md` も「Windows のタスクバー」）。

git-qa のデスクトップ検証は **macOS 専用**（`CGWindowListCopyWindowInfo` /
System Events / Vision OCR）。**噛み合わないと思ったが、実際に起動したら macOS で動いた。**

```
pnpm install   # Electron 本体 225MB
pnpm run dev   # 窓が出た。持ち主の名前 Electron / 題 ローカル連動くん
```

### 分かったこと 1 —— **検証シートの形式はもう同じだった**

`docs/test-specs/*.tsv` の 1 行目が `#! md-business:test-spec-tsv/v1`。
**git-qa がそのまま読める。**変換は要らない。

### 分かったこと 2 —— **既存の 55 行は、1 行も動かせなかった**

| | |
|---|---|
| 行 | 55 |
| 手順 | 96（**落とせない 88**） |
| 期待結果に鉤括弧が 0 個 | **35 行**（画面で探す文字が無い） |
| そのまま動かせる行 | **0 / 55**（「押す」を足して **1**） |

**これはバグではない。**あのシートは人が見るために書かれている
（`node_modules と out を削除する` / `画面を 3 秒見せる` / `色を見比べる` / `Win+← を押す`）。
**git-qa が代わってはいけないものが、そのまま並んでいる。**

落ちた中に語彙の問題が 1 つ混じっていたので直した（`6d1261b`）。
**日本語の button は「押す」。**鉤括弧は必須にした —— `Enter を押す` はキーの話で、
画面の文字ではない。

### 分かったこと 3 —— **Electron は AX に中身を出さない**

窓の部品を数えたら **11 個**で、全部が `group` と OS のウィンドウボタン。
**画面の文字が 1 つも取れない。**

段 2（OCR）を足すと **27 行 / 242 文字**読めた。

```
ローカル連動くん / 緊急停止 / 観測地点を選ぶ / 対象を追加する
カレンダーで別の日を選ぶ / 一覧 / カレンダー / ローカル動作
```

**Electron を相手にするなら、段 2 は「あると良い」ではなく「無いと何も読めない」。**
配布物へ OCR を同梱しておいたのは正解だった（上記「着手前に塞いだこと」）。

OCR の癖も見えた: `⚙ 管理` → `•管理`、`9月7日` → `19月7日`。
**期待結果には、OCR が実際に読めた文字だけを書く。**

### 分かったこと 4 —— **見る許可と触る許可は別**

新しく起こしたシート 6 件を当てた結果（**下書き。人はまだ見ていない**）。

| | 探した文字 | 下書き |
|---|---|---|
| 1 | ローカル連動くん | PASS |
| 2 | 緊急停止 | PASS |
| 3 | 観測地点を選ぶ | PASS |
| 4 | 対象を追加する | PASS |
| 5 | カレンダーで別の日を選ぶ | PASS |
| 6 | 「管理」を押す → 管理・診断 | **BLOCKED** |

6 件目の言い分がこれだった。

```
osascript が失敗した: 36:56: execution error: System Events でエラーが起きました:
osascript には補助アクセスは許可されません。 (-25211)
```

**原因は書いてあるのに、直し方が無い。**しかも画面収録（見る側）は通っているので、
これを読んだ人は画面収録の設定を疑いに行く。**直した**（`38ccb2c`）。

## 人がやること（**ここで止まっている**）

1. **システム設定 → プライバシーとセキュリティ → アクセシビリティ**に
   ターミナルを足して入にする。**足したらターミナルを開き直す**
2. `pnpm run:sheet:desktop sheets/renraku.tsv` を走らせ、**6 件を見て置く**
3. 既存の 55 行をどうするか決める
   （**機械に流すものと、人が見るものを別のシートに割るのが素直**だと考えている）

## 作業ログ

- 2026-09-07 ベガスギフト側へ Issue #30 を出した。**返事はまだ無い。**
- 2026-09-07 連動くんを macOS で起動し、git-qa から読めることを確かめた。
  **走らせるシート（6 件）を `sheets/renraku.tsv` に起こした**（public には出さない）。
  **5 件が下書きで PASS。6 件目は触る許可が無くて止まっている。**
