# 017 — 通信ログを拾って画面に出す／LLM の判断を入れる

- Phase: 2
- 前提: 015（ウェブ検証）が試験運用に入っていること
- 優先度: **試験運用のフィードバックが出てから決める**

## 何を言われたか（2026-09-06・**人の言葉のまま**）

> LLM 判断も含めて tsv をみながら一緒に操作をみれて、判定できるのは実に面白い機能だと思います。

> もっというと実機アプリの通信ログ mmtp? とかもキャッチして git-qa 上に出力しつつ
> LLM も見れて判定できるとか

## いまの実態（誤解を避けるために書いておく）

**判定に LLM は使っていない。**画面に「AI の判定」と出ているが、中身は

- 手順の解釈 = **正規表現 6 本**
- 期待結果 = `screenText.includes(check.text)` の 1 行

**実行中に外の頭脳を呼んでいる箇所は 0。**

## 考えられる形（決めていない）

1. **通信を拾う** — ウェブは CDP の `Network.*` でそのまま取れる（**依存を足さずに済む**）。
   実機アプリは間に立つ必要があり、証明書の話が出る。**難しさが桁で違う**
2. **画面に出す** — 3 カラムのどこへ置くか。判定の欄は既に埋まっている
3. **LLM に見せる** — 何を渡すか（画面の文字 / DOM / 通信）と、**その判断を証跡のどこに置くか。**
   `VERIFIED` は人が置いたものだけ（C1 / C17）。**LLM の判断は 3 本目の列**になる

## 決める順番

**試験運用のフィードバックを見てから。**人がここで書いた通り:

> 遅かれ早かれきにして、実務で確かめることにはなります。
> ただ試験運用をさっさと始めた方がもっとみのあるフィードバックが上がると思います。

**先に配る。**
