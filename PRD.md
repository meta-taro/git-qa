# git-qa — PRD（叩き）

- 作成: 2026-08-14
- 初期の企画草案 2 本をマージしたもの（草案そのものは非公開）

---

## 0. 原案 2 本の扱い

**2 本は同じ製品を別の範囲で書いたもので、どちらも上位互換ではない。**片方を捨てると情報が落ちるので、この PRD は両方をマージしている。

| | mobile-qa-runner | git-qa-expanded |
|---|---|---|
| 対象 | Android / iOS | Web + Android + iOS + Desktop + API / CLI |
| そちらにしかない中身 | Human Takeover の操作語彙 / Finding（テスト中の別発見） / 3 カラム UI / `human.*` MCP / run.json の項目 / 着手タスク 15 本 | Adapter アーキテクチャ / 権限を Git に委譲 / Web Adapter / 想定ユーザー / MVP 成功条件 / リポ名候補 |

**組み替えたのは順序と主媒体の 2 点。**理由は §2 と §6。

---

## 1. この製品は誰のためのものか

**検証する本人のため。**

一次的な価値は「自分で動作を保証する作業を、AI を使っていかに楽にするか」。証跡が第三者や顧客から読めることは**副次**であって、目的ではない。

**ここを取り違えると製品が壊れる。**証跡の提出を主目的に置くと、「証跡を整えるための作業」が増える。**本人の手間が減らないなら、この道具は使われない。**

### AI に丸投げしない、という前提

AI に全部やらせて全部通ったことにする道具ではない。**丸投げして事故が起きたとき、責任を持つのは人**であり、「AI がやりました」は通らない。

だからこの製品は、**人が責任を持つ前提のまま、その責任を果たすコストを下げる**ことをする。AI は代わりに判断するのではなく、**見るための準備をして、見る場所を絞る。**

---

## 2. 主媒体はライブビュー（原案から動かした一番大きな点）

原案は 2 本とも「1 ケース 1 動画」を証跡の中心に置いている。**実行中の人の体験としては、これは最悪の形式。**シークして、待って、見逃してもう一度戻る。手でテストするより遅い。

**実際は、人は AI が操作しているのをその場でライブで見ている。**

| いつ | 誰 | 媒体 |
|---|---|---|
| **実行中** | **本人** | **ライブビュー（主役）** |
| 直後 | 本人 | 落ちたところの静止画 |
| 後から | 本人「あれどうだったっけ」 | 動画 |
| 後から | 第三者・顧客 | 動画 |

### 帰結 1: UI の中心は録画プレイヤーではない

3 カラムの中央に置くのは、**いま動いている画面のミラーリング**。

### 帰結 2: 遅延がそのまま体験の質になる

1〜2 秒遅れると「AI が固まったのか、遅いだけなのか」が人には分からない。**分からないと人は手を出す。**そうなった時点で、この道具は目的を達していない。

- Android — **scrcpy 前提**（低遅延）。原案は「録画・ミラーリング候補」として横並びに挙げているが、ライブが芯なら候補ではなく前提
- Web — headed のブラウザをそのまま見せる（ミラーリング自体が不要）
- **Appium のスクリーンショット連写は使わない**（遅すぎてライブにならない）

### 帰結 3: 対象の線引きが決まる

**ライブ映像を出せるものが Adapter に入る。**

| 対象 | ライブ映像 | 扱い |
|---|---|---|
| Web | 出せる | Adapter |
| Android | 出せる | Adapter |
| iOS | **未知（D1）** | Adapter に入るかがここで決まる |
| デスクトップアプリ | 出せる | Adapter |
| API / CLI / バッチ | **出せない** | 別扱い。ログのライブ追尾という別の見せ方が要る |

原案 `git-qa-expanded` §21 が Phase 8 で API / CLI を足そうとしているが、**同じ Adapter には入らない。**

---

## 3. 人が見るのは 3 つだけ

全ケースを人が確認する道具にすると使われなくなる。検証シートが「質の悪い形式記録のノルマ」になったときと同じ形。

人が見るのは、

- **AI が判断できないと言ったところ**
- **前回と差分が出たところ**
- **落ちたところ**

残りは `AUTO_PASS` のまま置く。

**ASSIST モードの本体は「AI が全部やる」ではなく「AI が見る場所を絞る」。**原案 `mobile-qa-runner` §7 が ASSIST を中心モードと書いているのは、この意味に取る。

---

## 4. 結果の語彙

原案 `git-qa-expanded` の語彙を採る。

| 値 | 意味 |
|---|---|
| `VERIFIED` | **人が名前を置いた** |
| `AUTO_PASS` | AI が通した。**誰も見ていない** |
| `FAIL` | 落ちた |
| `BLOCKED` | 前提が整わず実行できなかった |
| `SKIP` | 実行しなかった |

`mobile-qa-runner` の素の `PASS` だと、上 2 つが同じ値に潰れる。**潰すとこの製品の意味がなくなる。**

### 不正防止機構は作らない（非目標）

`VERIFIED` を、中身を見ずに置くことはできる。**それは仕様であって欠陥ではない。**

置いた瞬間に、その人の名前が証跡に残る。**ロックではなく署名。**丸投げして事故が起きたときに困るのは置いた本人なので、道具側で防ぐ必要がない。

**明記しておかないと、実装する側が善意で承認フローを作り始めて、楽をするための道具が重くなる。**

---

## 5. 操作コストを数える

判断が正しくても、**そこに至るまでの手数が多ければ楽になっていない。**

- **`VERIFIED` は 1 打鍵で置ける**
- **置いたら次のケースへ自動で進む**
- ケースごとに動画をシークしてボタンを探す、という導線を作らない

ここは MVP の要件に入れる。

---

## 6. 成功をどう測るか

原案 `git-qa-expanded` §22 の MVP 成功条件に「従来の手動チェックより明確に楽」とあるが、主観のままでは判定できない。**これが主指標なので、測れる形にする。**

> **同じ 20 ケースを、手で通した所要時間と、この道具で通した所要時間。後者が明確に短いこと。**

実案件が手元にあるので測れる（Issue 008）。

**この数字が出ないなら、他が全部できていても MVP ではない。**

---

## 7. TSV の扱い

テストケースは TSV。md-business と同形式。

**ただし git-qa は TSV を読むだけで、書き戻さない。**

- 結果は `run.json` に出す
- TSV への反映は **md-business 側が行う**（行 ID・`computed`・`hidden` を知っているのはあちら）
- エージェントが両方を呼んで橋渡しする

**理由は 2 つ。**

1. **OSS 側が薄くなる。**TSV の書式規約を git-qa が抱え込まないので、md-business が仕様を変えても追随不要
2. **md-business を持っていない利用者を壊さない。**行 ID も computed 列も知らないまま書けば、集計列を実数で潰す・控えの行を復活させるといった壊し方をする。**読める形のまま壊れるので気づけない**

---

## 8. 録画は設定で決める

**録画の on/off は実行開始時の設定項目。モードには紐づけない。**どのモードで録画が要るかは、その時の人が決める。

1 点だけ必須要件がある。**`run.json` に「録画しなかった」を残すこと。**動画が無いとき、それが「録画オフで走らせた」のか「録画したはずが失敗した」のかを区別できないと、後から証跡として読めない。

---

## 9. 唯一の未知は Windows → iOS

**この製品で分かっていないのは 1 つだけ。**

| 組み合わせ | 状態 |
|---|---|
| Windows + Web | できる（Playwright） |
| Windows + Android | できる（ADB / scrcpy） |
| **Windows + iOS** | **未知** |
| macOS + Android / iOS | できる算段がある |

**最悪ケースでも製品は成立する。**

原案 `mobile-qa-runner` は同じ事実を「**だからプロジェクト全体の成立条件にはしない**」と書き、**後回しの根拠**に使っている（§4）。

**逆に使う。**死なないから、**いま試せる。**

そして問いは操作ではない。

> **Windows から iOS の画面を、実用的な遅延でリアルタイムに見られるか。**

操作だけできても、本人が見られなければ ASSIST が成立しない。**iOS が AUTO 専用になるなら、それは製品として別物。**

### まず挑戦する。駄目だったら Windows のまま出す（C13）

**順序を間違えないこと。挑戦が先。**

- **成立** — そのまま Windows で iOS Adapter を足す
- **条件付き** — **条件を潰す方向で検討する。**ここで即座に落とさない
- **不成立** — **Windows 版は iOS なしで出す。**macOS 専用にはしない

**乗り換えるのではなく、載せないだけ。**mac を前提にすると「Windows で動く道具」という前提そのものが崩れる。macOS 対応は将来の別枠で、MVP には入れない。

**この決定は「iOS を諦める」ものではない。**Issue 001 は Phase 0 の最初に実施する。**「載せない」は判定の結果であって、着手前の前提ではない。**

そのうえで、**結果が出てもロードマップと設計は揺れない。**決まるのは「Windows 版に iOS が載るかどうか」だけで、**機材の判断には繋がらない。**

---

## 10. 証跡と公開の線引き

証跡には**実画面が写る。**顧客名・電話番号・実在の店舗名が入る。

顧客へ返す前提のものなので、**その顧客自身のデータが写ること自体は問題にならない。**

守るのは 1 本だけ。

> **OSS の公開物に実証跡を混ぜない。**サンプル・ドキュメントはダミー案件で作る。

**OSS 公開は確定**（C15・2026-08-15 決定）。commit history に個人名・個人メールも残さない。検出 CI を叩きに同梱する。

**本体（公開）と証跡（非公開）は別リポ**（C16）。本体は history を消せるが、**証跡は history が中身なので消せない。**混ぜると、消せない側に消したいものが入る。

---

## 11. 非目標

原案の非目標を引き継ぎ、2 本足す。

- Appium / Maestro / scrcpy / Playwright の再実装
- クラウド Device Farm
- SaaS 化
- CI/CD の置き換え
- 独自 AI モデルの開発
- **不正防止機構**（§4）← 追加
- **全ケースの人間確認を強制する仕組み**（§3）← 追加

---

## 12. この製品の実験場は手元にある

同形式の検証シート（独自 TSV）は既に別プロダクトで運用しており、実際に人が記入している実績がある。

**§6 の主指標「実案件で 10〜20 ケース運用」は、探しに行かなくても手元にある。**
