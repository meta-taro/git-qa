# DESIGN.md — 見た目の方向

- 決めた日: 2026-09-03
- 決めた人: リポジトリの持ち主（**AI は決めない**・product-baseline §11）

## 立ち位置

**開発者の道具。**md-business / dbboard と同じ系列に見えること。

- 角丸の四角に白い印のアイコン
- 情報密度は高め。飾りより、いま何が起きているかが読めること
- 動きは最小限。状態が変わったことを伝えるときだけ

## 色

**緑。**「**見て、大丈夫だった**」を表す色。この道具が出すいちばん大事な結果が
「人が見て保証した」なので、そこに寄せる。

| 用途 | 値 |
|---|---|
| アイコンの地 | **`#34D399`**（白みがかったエメラルド） |
| 画面のアクセント（ライト） | `#059669` |
| 画面のアクセント（ダーク） | `#34D399` |
| アクセントの上に載る文字 | ライト `#FFFFFF` / ダーク `#04231B` |

**家族の中で見分けが付くこと**も条件にした。Dock に 3 つ並ぶため。

| アプリ | 主色 |
|---|---|
| md-business | `#5B5BD6`（藍） |
| dbboard | `#4F46E5`（藍） |
| **git-qa** | **緑** |

`#34D399` / `#059669` は **dbboard の `success` と同じ値**。同じ系列の中で「うまくいっている」を
表す色を共有する。

**使い所は絞る。**画面の中で「いま人がやること」を 1 つだけ強く出すために使う
（実行を始めるボタン、打鍵待ちのケース）。**それ以外は OS の色を使う。**

**白い印が沈まないことを確かめてある。**アイコンの地と白の明るさの差は 1.9:1 で、
Dock の大きさ（32px）でも読める（実測して選んだ）。

## アイコン

**角丸の四角に、端末の枠の中のチェック。**白い印、地は `#34D399`。

- **形が製品を説明している。**この道具の芯は「端末の画面を人が見て保証する」ことなので、
  枠＝画面を入れる。チェックだけだと、他の QA ツールと区別が付かない
- 元データ: `packages/desktop/src-tauri/icons/source.png`（1024px）。
  **描き方は `icons/make-icon.py` にある**ので、色や形を変えたいときはそこを直して
  `pnpm --filter @git-qa/desktop exec tauri icon src-tauri/icons/source.png` を走らせる
- **手描きの素材は使っていない。**外部の著作物を含まない（MIT のまま配れる）

## 明暗（ライト / ダーク）

**ライト / ダーク / システムに従う の 3 つ。**メニューの `表示 > 外観` から選ぶ。

**独自の配色を持たない。**切り替えるのは `color-scheme` とウィンドウの外観だけで、
実際の色は OS が持っているものを使う。枠線や陰影は `currentColor` から作る。

- 理由: 配色を自前で抱えると、OS の設定（コントラスト、色覚の補助）から外れる
- アクセント色だけは例外で、上の「使い所」に限って置く

## 字面と間隔

**まだ決めていない。**決まるまでは OS の既定（`font: menu`）を使う。

dbboard は Inter / JetBrains Mono・本文 13px・基準 4px・窓 8px・部品 6px で揃えている。
**同じにするかどうかは、実際に画面を見てから決める。**

## この文書の使われ方

- ここに書いていない色を、コードへ直接書かない（`#000` を直書きして真っ黒になった前例がある）
- 変えたときは、`packages/desktop/src/styles.css` と同じ commit で直す（§10）
