# 変更の記録

**まだ 1 度もリリースしていない。**`package.json` の版は `0.0.0` のまま。
下の日付は「その日に入った変更」で、**版ではない。**

**MCP の `about` がこの文書を読む。**書き方を変えるときは
`packages/mcp/src/about.ts` の検査も一緒に直すこと（`## 見出し` と `- 箇条書き` を読んでいる）。

## 未リリース

- 直した: **WebView を使うデスクトップアプリで、押したいものが軒並み見つからなかった**
  （外部レビュー #11）。AX の探索が深さ 6 で打ち切られていた ——
  **窓 → Group → ScrollArea → WebArea と潜るので、素の AppKit より 2〜3 段深い。**
  9 まで潜るようにし、`GIT_QA_AX_DEPTH` で上げられるようにした
- 直した: 「画面に見つからない要素」が、**無いのか届かなかったのかを区別していなかった。**
  打ち切っていたら、そう言う（読んだ人が「名前が違うのだろう」と別の名前を試していた）
- 直した: 段 2（OCR）の部分一致が**最初に当たった行**を採っていた。段 1 は最小を選ぶのに、
  ここだけ違っていた。**長い行にたまたま含まれる短い語を掴むのは、たいてい間違い**
- 足した: **キーボードの手順**（`Enter キーを押す` / `Ctrl+Enter キーを押す`・外部レビュー #6）。
  **鉤括弧＝画面の要素、という決まりは崩さない**（「キー」の 1 語で決める）。
  括弧なしの「Enter を押す」は今までどおり判断保留だが、**どう書けば動くかを言う**
- 直した: **キーが「実装されている」のに、実際には押せていなかった。**
  macOS は `keystroke "Enter"` で**「Enter」という 5 文字を打ち**、
  ウェブは `keyCode` が無いので**既定の動作が起きていなかった**（実物で確かめた ——
  `<form>` の中で Enter を送っても題が変わらなかった）。どちらも直した
- 足した: `AdapterCapabilities.keyInput`。**押せない相手には手順を回さない**
  （Windows のデスクトップはまだ `false`。走らせて落ちるより、人へ回すほうが読める）
- 直した: **1 本走らせると入口が二度と使えなかった**（外部レビュー #5）
- 直した: **掴まれたまま起こして、理由の分からない死に方をしていた**（外部レビュー #7）。
  誰が掴んでいるかと空け方を出す。**`vite` は IPv6 で待ち受ける**ので、そちらも見る
- 足した: **Windows のデスクトップ検証**（`git-qa-win`・Rust・Win32 と UI Automation）。
  窓を探す・撮る・文字を読む・押す、まで。**なぞる・文字を打つ・録るはまだ無い**
  （無いものは「無い」と名乗らせてある —— 文字の手順は「人が入力する」に倒れる）。
  **まだ 1 度も走らせていない**（書いた側は Windows 機を持っていない）。
  引き継ぎは `docs/windows-desktop.md`
- 足した: `AdapterCapabilities.textInput` に **`'none'`**（文字を送る口を持たない）。
  `ascii-only` と名乗らせると、**送れないものを送ろうとして実行時に落ちる**
- 直した: **`adb` が入っていない機械では、入口の画面が出なかった。**
  端末の一覧で `ENOENT` が投げられ、`/state` が**応答を返さないまま固まる** ——
  画面は「読み込み中」のまま、理由も出ない。**Android を見ないなら URL だけで
  始められるのに、入口で止まっていた。**理由を出して、他の道は残すようにした
- 直した: **`.dmg` は aarch64 なのに、同梱の道具が x86_64 だった**（配る直前に気づいた）。
  建てた端末の Terminal が Rosetta の下にいたため、`swiftc` が x86_64 を選んでいた。
  **Rosetta を入れていない Mac では、文字が読めず録画もできない** ——
  しかもエラーではなく「できない」として出る。**両方の CPU 向けに建てて 1 本にした**
- 足した: **配布物を建てて Release に載せる流れ**（`release.yml`）と、
  **macOS の署名・公証の手順**（`docs/signing.md`）。
  **秘密情報は 1 つも書いていない**（名前と、どこで作るかだけ・§14）。
  `.app` の中の道具 3 つにも署名する —— **1 つでも未署名なら公証は通らない**
- 直した: **配布ページのアイコンが、Tauri の雛形のものだった。**
  `icons/128x128-2x.png` が**最初の commit から中身を替えられておらず**、
  しかも**配布物のアイコン一覧にも入っていた**（`tauri icon` はアットマーク付きの
  名前にしか書かないため、この名前だけ取り残されていた）
- 直した: 証跡に書くパスが、**その機械の事情を持ち込んでいた。**
  Android の録画は絶対パス（＝個人名）、ワークスペース相対のパスは Windows で
  区切りが `\` になっていた。**どちらも `case-001/screen.mp4` の形に揃えた**
- 足した: **試験 1 本を 1 つのフォルダにまとめる**（ワークスペース・`pnpm ws:init`）。
  検証シート・証跡・画面・動画が同じ所に積まれるので、**フォルダごと git で管理できる**
- 足した: **続きから走らせる。**途中で止めた実行を選ぶと、**同じ証跡に足す** ——
  昨日人が見て置いた分を、もう一度置き直さない
- 変えた: **走らせていないケースは、証跡に書かない**（前は「止めたので BLOCKED」と
  書いていた）。**走らせた末に判断保留になったケースと見分けが付かず**、
  続きから走らせるときに、どれをやり直すべきかが読めなかった
- 直した: 証跡の `sheet.path` に**その機械の絶対パス**（＝個人名）が入っていた。
  ワークスペースからの相対にした
- 直した: **途中で止まった証跡が、走り切ったものと見分けが付かなかった。**
  「1 件終わるたびに書く」（外部レビュー #2）が、途中経過にも `finishedAt` を押していた。
  `finishedAt` は「**無いことが『途中で止まった』の記録になる**」という約束なので、
  走り切ったときにだけ押すようにした。
  **証跡が残ることと、残った証跡が何なのかが読めることは別の話。**
- 足した: **配布ページ**（GitHub Pages・`site/`）。版と落とす先は**ページの JS が Releases から
  読む** —— 埋め込むと、タグを打つたびにページを直す作業が増え、**忘れた分だけ古い物を配る**
- 直した: **Windows では `pnpm verify` が 12 件落ちていた。**検査が `/docs/a.tsv` のように
  POSIX の区切りを直書きしていたため（実装は `path.join` で正しかった）。
  **Windows 版を初めて建てて分かった。**見たいのは「どこを指すか」であって、
  区切りが `/` か `\` かではない
- 変えた: 版を **0.1.0** にした（それまで `0.0.0`）。アルファ版として配り始めるため
- 直した: **Windows 版は、押した瞬間に落ちる状態だった。**`tauri.conf.json` が
  macOS 専用の道具 3 つ（Vision / CoreGraphics / ScreenCaptureKit）を**無条件で参照**
  していたので、**建つ前に `pnpm check:assets` で止まる。**`tauri.macos.conf.json` へ分けた
  （Windows で動かすのはウェブ検証と Android 検証で、**そもそもこの 3 つは要らない**）
- 足した: **鑑賞モード**（`mode: 'watched'`）。**人が押さなくても 1 件ごとに間をおいて進む。**
  押せばその判定になり、押さなければ `AUTO_PASS`（**繰り上げない**）。
  **Esc と押しボタンで止められる** —— 見ているだけの人が止められないのは、
  見ているだけより悪い。`assisted`（押すまで待つ）とも `auto`（誰も見ていない）とも別の名前にした
- 足した: **git-qa の窓の録画**（`git-qa-record`・ScreenCaptureKit・macOS 専用）。
  **相手のアプリだけ録っても、判定の根拠は写らない** —— ライブ映像・いま何を判定して
  いたか・AI の言い分・矢印が写るのは git-qa の窓のほう。
  ffmpeg が**在れば** webm(VP9) にする（実測 169,644 → 37,936 bytes）
- 直した: **その指紋が、実物では 1 度も取れていなかった。**`NSRunningApplication` は
  Foundation ではなく AppKit で、JXA は毎回 `TypeError` を返していた。
  呼び側が失敗を `undefined` に畳んでいたので、**証跡には「測る口を持っていない」と出ていた** ——
  口はあったのに。**文字列の形だけを見る検査では捕まらない。**実物で動かす検査を足した
- 足した: **走行中に検証対象が入れ替わっていないか**を証跡に残す（`targetCheck`・外部レビュー #3）。
  **止めない。**変わったことが読めれば足りる。
  **「変わっていない」と「測れなかった」を混ぜない**（測る口の無い相手は `unmeasurable`）
- 直した: **途中で落ちると、人が置いた判定が全部消えていた**（外部レビュー #2）。
  1 件終わるたびに証跡を書く。**signal を拾う道は効かない** —— 手元の CLI は
  `vite-node` の下で走り、**signal がスクリプトまで来ない**（実測）
- 直した: `tauri dev` の watcher が、**自分で作った `resources/` を見て画面を建て直していた**。
  人が判定を置いている最中に再起動していた（同 #2 の補足）
- 足した: 証跡の絵を **webp** で残す。ウェブは CDP がそのまま出し、デスクトップと Android は
  `cwebp` か `ffmpeg` が**在れば**変換する（**無ければ撮れた形のまま。前提を増やさない**）。
  実測 231,433 bytes → 26,786 bytes
- 足した: **判定を置く時点の画面を、ケースごとに残す**（`case-001/screen.webp`）。
  それまで証跡フォルダには `run.json` しか無く、**「あとで見直す」材料が 1 枚も無かった**
- 直した: 撮った絵の**名乗りと中身が食い違っていた**。デスクトップは JPEG を返しながら
  `png` と名乗り、MCP はそれを `image/png` として AI エージェントへ渡していた
- 足した: 配布物が参照しているものが実在するかを数える検査（`pnpm check:assets`）。
  **建てる手順が失敗を飲み込む**のに、誰も数えていなかった（product-baseline §23）。
  **配る直前にだけ数える** —— 普段のゲートに入れると、建てる道具が無い環境で開発が止まる
- 直した: 建てて作るはずのバイナリ（`git-qa-ocr`）が Git に入っていた。
  `.gitignore` に足したのが後だったので、追跡が外れていなかった
- 足した: 証跡が何に対して置かれたのかを確かめる口（`pnpm check:run`）。
  シートの `sha256` は書く側が 4 箇所あったのに、**読む側が 0 件**だった（外部レビュー #1）
- 足した: この道具の説明を AI エージェントへ返す口（MCP の `about`）。版ごとの変更も返す
- 足した: AI が触った場所を「ここ」と指す矢印。斜めから指し、指すものの外へ置く
- 足した: 掴んで運ぶ（DnD）。デスクトップでは実装が無かった
- 足した: 前面に出さずに押す道具（`git-qa-input`・Rust・依存 0）。1 押し 3 秒超 → 99〜168 ms
- 足した: ウェブ検証を Firefox（WebDriver BiDi）と Safari（WebDriver）でも動かせるようにした
- 足した: デスクトップアプリの検証（AX → OCR の 2 段）
- 直した: スクロールが一度も効いていなかった（`System Events` に `scroll` は無い）
- 直した: 押した操作・回した操作を、理由を言わずに捨てていた
- 直した: 置けていないのに「置いた」合図が光っていた
- 直した: 矢印の説明が「判定は動かない」で、置き直せることが伝わっていなかった
- 直した: 触る許可の説明（`-25211`）が、合っている設定を疑わせていた
- 直した: 窓が見つからないときの文言が「無い」と「見えていない」を区別していなかった
- 直した: 検証シートの手順で「押す」が読めなかった（日本語の button は「押す」）
- 直した: ハンドルが日本語を受け付けなかった
- 変えた: 記録から他の案件の名前を外した。**残すのは踏んだ事実の方**（C58）
- 変えた: 品質ゲートの出力を絞って見ない（C59）。絞ったせいで lint の失敗を 2 回見落とした

## 2026-09-08

- 足した: 矢印（要望シート No.1）。人に 6 回直してもらった
- 直した: 指すものの大きさを実行器が捨てていて、矢印が対象の上に載っていた

## 2026-09-07

- 足した: 前面に出さずに押す道具。掴んで運ぶ
- 直した: 押す・なぞるで相手が前面に出る、指が飛ぶ、といった触り心地
- 変えた: 配布物のやり取りをやめ、クローンして動かす形にした

## 2026-09-06

- 足した: ウェブ検証（Chrome / Firefox / Safari）。ブラウザの版を証跡に残す
- 足した: デスクトップアプリの検証
