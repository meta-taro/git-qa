{
  "name": "@git-qa/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "検証中の画面をライブで見て、その場で判定を置くためのデスクトップ画面",
  "license": "MIT",
  "dependencies": {
    "@git-qa/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc --build && vite build",
    "dev": "vite",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "happy-dom": "^20.12.0",
    "vite": "^7.3.6"
  }
}
